# STATUS — current snapshot (details in HANDOFF.md + docs/phases/)

- **Env:** `C:\conda-envs\drlca-rag` only.
- **Done:** OCR 27/27 (clauses 1–58, PARTs I–X) · retrieval baseline 10/10 · env pinned · docs pass.
- **Phase 07 prep 2026-09-09 (superseded — see LIVE line below):** requirements slimmed + clean-venv proven; README + ASCII arch; NAB/NNAD HIGH 10/10; spec audit in journal; key-audit clean.
- **Phase 02 DONE 2026-09-09 (v2 + fix-A runs) + fixA2 COMPLETE 2026-09-10:** v2 12/12 (8/10 cited; Q9 PARTIAL s.33→s.34, Q10 false refusal); fix-A run 8/12 (quota 20/day/model confirmed by 429; Q8/Q9/R1/R2 honest-pending) — Q10 VARIANCE RESOLVED (verbatim s.46 quotes, PASS), Q1-Q7 stable (Q4 +padding note), Q5 correct-refusal x3. fixA2 2026-09-10 12/12 (reviewer SEND-BACK dispositions recorded): Q9 s.33 REMOVED but s.34 omission remains, Q3 recovered (cl.29,30 5%), Q8 five-years, Q10 PARTIAL (s.46 x2 correct + s.39 misattr of High-Court text — TOC-trap class), R1/R2 verbatim DRAC refusal, Q5 correct-refusal x4. Offline: recall 0.800 PASS, faith 1.000 mechanical (overstates Q10), reverse_rel 0.630 FAIL gated (short-answer artifact, judge arbiter). Flagged rerun `eval_phase06_results_fixA2-flagged_2026-09-10.json`: faith 0.967 PASS (Q10 s.39 flagged), pointer swapped to fixA2.
- **Phase 03 DONE 2026-09-08:** 10/10 rows verified (2 pruned), 16/16 matrix + 7/7 statics PASS (reviewer: ship-with-notes). Residual: single-token fuzzy misroutes death/blend → Phase 04 must confirm on fuzzy flag. Next: `docs/phases/04_intent_router.md`.
- **Phase 04 DONE 2026-09-08:** keyword router 10/10 (exit needs 8), clarify path proven, mean latency ~0.6ms (reviewer: ship-with-notes + fixes applied: cue anchoring, singleton guard, tie policy). LLM router rejected (keyword suffices, quota preserved). Next: `docs/phases/05_accessible_ui.md`.
- **Phase 05 DONE 2026-09-08 (+styling pass) + dark-alert fix 2026-09-09:** app.py live-verified in Chromium light+dark (legal/help flows, routing trap, citations, helplines; 360px banner OK); .streamlit/config.toml light/dark Nigerian-green theme; watermark bundled (static/watermark.jpg, CC BY 4.0); scripts/test_phase05.py 104/104 (grew 80→94→96→104 across sessions; +scrims/stacking/watcher/HC-button/greeting asserts); boot HTTP 200. Residuals FIXED: (1) dark stAlert translucency → opaque override (#45491f, 9.1:1); (2) Brave white-text/sidebar report → opaque sidebar (z-index 2 over main context) + 0.88 main sheet + opaque excerpt cards; light/HC verified coherent. Font unchanged by design. Human-only left: NVDA, physical keyboard run, live mic/read-aloud, Render cold-start. Next: `docs/phases/06_ragas_eval.md`.
- **Corpus:** Act full 40K + factsheet 21K + Constitution 529K chars. `_SAMPLE_DO_NOT_CITE.txt` never citable.
- **Weak spot:** Constitution queries fixed by const size 400 (Q9 0.133→0.167 PASS, mean 0.221→0.275). Watch: constitution now 95% of joint index — per-doc routing is a Phase 02 follow-up.
- **Phase 07 deploy LIVE 2026-09-10 (Render):** service `srv-dah26opt0dsc73e9a350` (free, python 3.11, Oregon) at **https://ngodisabilityrag.onrender.com** — HTTP 200, browser-verified banner + help query (NAB, helplines first; 15-min sleep/wake cycle not yet observed). HF Spaces dropped (Streamlit SDK deprecated → Docker/PRO only). Streamlit Cloud still optional 2nd link.
- Git: `main` pushed clean 2026-09-10. Deploy audit + offline suites re-verified 2026-09-10: bench PASS, 03 16/16+7/7, 04 10/10, 05 104/104, boot import OK.
- **Hello-bug fix 2026-09-10:** bare greetings hit the refusal wall (live-reproduced) → `is_greeting()` in router + greeting panel in app (wins even over forced buttons, zero retrieval/LLM calls); 04 greeting probes + 05 asserts green; live-verified on Render.
- **Phase 06 CLOSED 2026-09-11 (judge arbitrated):** judge `gemini-2.5-flash-lite` on generator `gemini-2.5-flash` output (21 verdicts, artifact `scripts/judge_phase06_results_gemini-2.5-flash-lite_2026-09-11.json`). reverse_rel 0.630 FAIL = **metric artifact**, retired not chased: deterministic ceiling analysis caps a *perfect* system at **0.852** vs a 0.80 gate (Q3/Q8 hard-capped 0.333 by per-doc merging), and the judge rates 9/9 answers relevant — proxy-vs-judge agreement only **2/9**. Caveat recorded: all 9 judge scores were exactly 1.0 (assent bias), so the **arithmetic** carries the verdict, not the model. Q3 thinness confirmed a REAL prompt/generation defect (→ Phase 09); Q8 adequate. Judge limits logged (conflated Factsheet `Section 39` with Constitution `s. 39`; misread the `general` tag). Open + not claimed: two-run flakiness check, manual-100% (Q10 s.39 unsupported).
- **Phase 08 steps 1/2/3/6a DONE 2026-09-11, LIVE on Render (`main` @ `fcd2129`):** synonym map · cite-constrain fix-B · judge · answer cache. **recall 0.800 → 0.925** (Q5 0.000→0.250 blind spot opened; Q8 + Q9 0.500→1.000 unplanned wins from real `transitional`/`transitory` + `dignity`/`degrading` mismatches). **faith_audited 0.967 → 0.867 is the improvement, not a regression**: the old figure leaned on a hand-written `MANUAL_FLAGS` list, now `MANUAL_FLAGS == []` and Q10's s.39 misattribution is caught mechanically (auto 1.000→0.667), independently confirmed by the judge. Q5 faith 1.000→0.000 is a **frozen-transcript pool effect**, not a quality drop — the eval recomputes retrieval live, so the pre-M1 refusal is no longer justified now that `act2018:1,2` retrieve; **0.867 therefore understates current quality**. `MIN_SCORE`, `app.py`, `data/`, config.toml untouched; **`requirements.txt` byte-identical to pre-M1 (blob `dc312fa`, verified)**. Live smoke zero-quota: banner ✔, Q5 "penalties" → `[Act cl. 2]`/`[Act cl. 1]` matching local to 3dp (proves new retrieval deployed) ✔, help → helplines-first + NAB ✔, greeting `"hi"` beats a forced Legal button ✔.
- **Demo assets 2026-09-11:** 5 frames @1925×881 from the LIVE app + `docs/demo_storyboard.md` (frames 02/03 re-shot — first pass captured the page scrolled to the top and showed neither the citation tags nor the NGO list). Storyboard records the frames were shot at `db3a1fc`, i.e. they show the UX contract, **not** the Phase 08 retrieval gain.
- **Phase 09 PLANNED 2026-09-11, not started (`docs/phases/09_evidence_and_generation.md`):** a planning pass on the deferred Phase 08 steps 4/5 found three code facts that reorder the backlog. (1) **Steps 4/5 as written cannot reach a real user** — both need a model at *query* time (step 4 a cross-encoder; step 5 to embed the incoming query, which prebuilt chunk vectors do not solve) and `requirements.txt` bans ONNX/FAISS, so "flag-gated" means OFF in production. **Superseded**, not deferred again. (2) **Retrieval is no longer the bottleneck** — recall 0.925 vs a 0.75 gate, while the judge confirmed Q3 thinness is a REAL prompt defect (Q3 recall 1.000). (3) **The synonym map is fitted to the set that scores it** — entries kept/deleted by their effect on Q5/Q7/Q8/Q9 across only 10 questions, so 0.925 cannot distinguish generalization from memorization. Owner confirmed all four workstreams under a **runtime-shippable-only** constraint: (1) ops hardening (`--no-cache` + daily-cap-only flash-lite failover → real budget is 40/day, not 20), (2) `data/eval/questions.json` + ~30 held-out questions incl. first-ever false-refusal/false-answer measurement (frozen 10Q stays frozen AND separate), (3) BM25 re-rank *inside* the cosine gate + offline-computed `synonyms_auto.json` (both wins of steps 4/5, zero query-time deps), (4) Q3 answer-shape floor + named reverse_rel fix in its own commit + fresh 12-call generation pass.
- **Phase 09 steps 1+2 DONE 2026-09-13 (zero Gemini calls; PRs #9 `phase09/ops-hardening`, #10 `phase09/evidence-base`):** **THE FINDING — held-out recall 0.420 (n=25) vs frozen-10 0.925, delta −0.505.** The 2026-09-11 suspicion ("the synonym map is fitted to the set that scores it") is now measured: 0.925 was mostly fitting. Per class the shape is the tell — **vocab-mismatch 0.312** (n=8), cross-document 0.429, toc-trap 0.500, odd-wording 0.500: *the worst class is the one the synonym map exists to fix*, so **the hand-written map does not generalize**. Two numbers measured for the first time: **false-refusal 0/25 = 0.0%** on answerable held-out questions (frozen-10 0/10 — the floor is not denying help to PWDs; ranking is the problem, not the gate), and **gate-level false-answer 5/5 = 100%** of off-corpus questions clear `MIN_SCORE` — reported **ungated with the caveat, and `MIN_SCORE` untouched** (weak-overlap floor, not a semantic filter; bands inverted per `src/retrieve.py:26-44`; "sourdough" 0.125 / "maritime" 0.318 predate this work; the semantic layer is the strict-prompt `NO_ANSWER_SENTENCE` path and costs quota to measure). Step 1: `FALLBACK_MODEL=gemini-2.5-flash-lite` (separate free-tier pool → real budget **40/day, not 20**), `is_per_minute_429`/`retry_delay` moved verbatim into `src/rag.py` (judge imports+aliases, output byte-unchanged), `ask(use_cache=, failover=)` records `model_used` while `route["model"]` keeps its old meaning; failover is opt-in, fires once, **never on a per-minute 429**. New `scripts/test_phase09_ops.py` **38/38, zero network** — failover proven by *injection* (proving it live means deliberately exhausting a daily cap). Step 2: `data/eval/questions.json` (10 frozen + 30 held-out), `scripts/evalset.py`, `scripts/eval_heldout.py`; `eval_phase06.py` output **byte-identical** across the refactor (16335 B both, baseline `scripts/eval_p09pre.json`). `load_questions()` **KEPT, not retired** (contra the playbook): two independent sources that must agree > one nobody checks. Verified no held-out score is inflated by the `const_ref` schedule-item artifact. Corpus gap found: **Act cl.19 is uncitable** (OCR `19.1` → no `act2018` ref carries 19). `MIN_SCORE`, `app.py`, `requirements.txt`, `data/processed/` untouched.
- **Pending:** Phase 09 steps 3 (BM25 re-rank inside the gate + offline `synonyms_auto.json` — now has a real target *and* an uncontaminated yardstick) and 4 (Q3 generation defect), in that order. Quota: two-run flakiness check **unblocked but NOT run** (24 calls; `--no-cache` landed 2026-09-13); + AI-expander live smoke. Owner: GIF encode + NVDA/keyboard/mic gates + LinkedIn. Housekeeping: stray 0-byte `D:NGORAG_review_judge.diff` in repo root needs manual removal (permission-blocked; in no commit). NGO 10/10 verified (NAB/NNAD HIGH 2026-09-09).
- **Phase 10 B (chat-core) DONE 2026-09-15 (zero Gemini calls, branch `phase10/chat-core`):** DRLCA becomes a chatbot, and the eval came first. *"So can they fire me?"* has no disability term and no statutory term, so **ellipsis is a RETRIEVAL problem, not a UI one** — which is why the multi-turn set was built and measured *before* the chat UI and before corpus v2/dense (tuning those on single-turn questions only would optimise a query distribution the chatbot never issues). New: `data/eval/conversations.json` (22 conversations / 62 turns / 51 refs, all corpus-verified), `scripts/chatset.py`, `src/chat.py` (`contextualise`, `merge_help_slots`, `TurnPayload`, `cross_turn_drift`), `history=` on `ask()`/`build_prompt()`, `scripts/eval_chat.py`, `scripts/baseline_chat_2026-09-15.txt`. **`chat_test` authored BLIND before `src/chat.py` existed** (10 conv / 27 turns — left at 27, not topped up, because adding turns once behaviour is visible is how a blind set stops being blind). **Results:** chat_dev **0.464 → 0.571 (+0.107)**, chat_test **0.435 → 0.565 (+0.130)**; ellipsis **0.000 → 0.333** (dev) / **0.200 → 0.600** (test); `direct` control **+0.000** both sets; recall@60 (pool k=20/doc) **0.714 → 0.929** / **0.783 → 0.913**. Help slots (`"I'm deaf"` … `"anywhere in Kano?"`) **3/5 → 5/5** and **1/2 → 2/2**. **False refusals FELL** (dev 1/28 → 0/28, test 3/23 → 1/23) with **0 contextualisation-induced** — the one retrieval number `eval_chat.py` fails on. **The new chat-only trap did NOT fire:** off-corpus-after-legal clears `MIN_SCORE` **2/2 in both arms on both sets, delta 0.000** (one probe's top score *fell* 0.2755 → 0.2434); that matches the published single-turn 5/5 and is a property of the inverted-band floor, **`MIN_SCORE` untouched**. **Recorded, not tuned:** chat_test's pronoun class did **not** improve (0.400 → 0.400), and a blind test-set note (`CT6.t3`) predicted a carry that behaves otherwise — the wrong note stays, because editing it after seeing behaviour is what spent the 30 held-out questions on 2026-09-13. Thresholds (`CARRY_WINDOW=2/THIN_MAX=3/MARKER_MAX=4/QUESTION_WEIGHT=2`) chosen on **chat_dev only**. `test_phase09_ops.py` 38 → **51 asserts**: `history=None` renders a **byte-identical** prompt (cache keys on the whole prompt) and history renders **BEFORE** the final `"Question: "` line (**privacy** — `_cache_write` stores only that line, and this population discloses abuse). Chat has its own `CHAT_PROMPT_VERSION="chat-cite-strict-v1"` + rule 8: citable evidence is **only this turn's excerpts**. **Unmeasured on purpose:** cross-turn citation drift (needs a generated answer → quota; wired, runs in Phase G). Untouched: `MIN_SCORE`, `src/router.py` (**still stateless**), `app.py`, `requirements.txt`, `data/processed/`, `questions.json`; `eval_phase06.py` output still `CE716FB3…5F19`; 01/03/04/05(104)/ablate_08/audit/heldout all green. Playbook: `docs/phases/11_chat.md`. **Next: Phase C — the chat UI (`phase10/chat-ui`).**
- **Phase 10 C (chat UI) DONE 2026-09-16 (zero Gemini calls, branch `phase10/chat-ui`):** Phase B's measured gain (+0.107 chat_dev / +0.130 chat_test strict recall) was **unreachable by a user** — `app.py` was still a single-turn form. This step spends it and measures nothing new: **no retrieval, scoring, corpus or prompt change**, so `eval_chat.py` and `eval_heldout.py` were required to reproduce `368f083` exactly and did (**byte-identical stdout**); `eval_phase06.py` still hashes `CE716FB3…5F19`; `git diff --stat main -- requirements.txt` **empty**. `app.py` splits into **`render_turn`** (computes + renders a new turn) / **`replay_turn`** (renders a stored `TurnPayload` — **no retrieval, no router, no network**, asserted) / `render_history` / a `st.chat_input`-driven `run()`, with `render_excerpts`/`render_ai_expander`/`render_help_records`/`render_plain_caption` shared so a live turn and a replayed turn are the same pixels. `src/router.py` gained **one defaulted arg** (`_help_payload(..., slots=None)`) and **stays stateless** — accumulated slots arrive as an argument, never stored. **`test_phase05.py` 104 → 137 asserts, all green**, 6 rescoped with an inline note each (`:32-39` `.index()`-ed into the whole module; `def render_helpline_banner` now sits *after* `def render_help` in file order, so the module-wide check would have failed on layout while the rendered order was unchanged — they now scope to `render_turn`'s body). **Two disclosed deviations from the plan:** (1) a **second** defaulted `TurnPayload` field `help_payload` — without it, replaying a help turn forces a live router call on every rerun, the exact thing `replay_turn` exists to prevent; neither new field is read by any eval, so no number can move; (2) the per-turn `answer_legal(..., use_llm=False)` **probe was deleted** — it existed only to print the prompt-version string and cost **a whole second retrieval per turn per rerun**; replaced by `_prompt_id()`, which reads `PROMPT_VERSION`/`CHAT_PROMPT_VERSION` from `src/rag.py`. **Width/pool depth was NOT folded in** (the older plan had it here): `offline_legal_hits`' docstring forbids a display path that slices differently from `ask()`, both harnesses measure `k=3/doc, top_n=6`, and Phase B measured the change at **test +0.000** — it moves in **Phase E**, with the harnesses, where the dense arm needs it. The **display** split (3 excerpts inline + rest in one expander) did land. **Privacy: history is `st.session_state` only.** New `scripts/.quota_log.json` holds **counts only** (`{"YYYY-MM-DD": {"model": 3}}`), fails open like `_cache_write`, gitignored; sidebar says *"THIS INSTANCE SINCE RESTART"*, never *"today"* (ephemeral FS + 15-min sleep). Cache replays are not counted — they spend no quota. **Browser-verified** (Chrome/DevTools, no key spent): 4-turn conversation with an ellipsis (*"so can they fire me?"* → `unbound-pronoun:they`), a clarify turn, and a narrowing (*"I'm deaf"* … *"anywhere in Kano?"* → NNAD from a slot carried two turns); banner at page top **and** leading every assistant turn; per-turn read-aloud **genuinely isolated** (each `components.html` is its own iframe — verified by reading `#drlca-speech` per frame); both AI toggles OFF; Clear conversation works; 360px no overflow; dark + HC coherent. **After the whole session neither `.answer_cache.json` nor `.quota_log.json` existed** — the offline path wrote nothing. Three AI-failure paths (no key / 429 daily cap / network down) proven **headlessly at zero quota**: `answer` stays `None` (pending, never faked), no quota recorded, one plain-language warning, no traceback. **Latent bug fixed in passing:** `try_voice_input()` wrote `session_state["q"]` *after* the `q`-keyed widget rendered, which Streamlit forbids; it now writes `voice_draft`, copied into `q` before instantiation. **Observed, not changed:** dark+HC together leaves the sidebar `#010409` not `#000` (equal specificity, dark rule emitted later; ~19:1 contrast, predates this phase; `accessibility_css()` untouched). Untouched: `MIN_SCORE`, `data/`, `requirements.txt`, `.streamlit/config.toml`, `read_aloud_html()` (byte-identical). Owner-gated: NVDA, physical keyboard run, live mic, AI-expander live smoke. **Fresh-eyes review: no blocking issues, verdict ship** — it re-ran 03/04/05 + `import app`, traced every `session_state[key]=` write against every widget `key=`, and confirmed history reaches no file. Two fixes taken (104 → **137**): (a) `_prompt_id()` decided multi-turn from `turn_index > 0` while `ask()` branches on `rag._render_history()` being non-empty — equivalent today only by caller discipline, since `history_for_prompt()` applies `CARRY_WINDOW` and drops blank turns; now `_multi_turn(prior)` calls the same two functions `ask()` calls, with 3 asserts pinning it (**the comment was the defect, the code was lucky**); (b) a **pre-existing tautological assert** — `no-wide-columns-for-banner` ended in `or True` and had been padding every published count since Phase 05 — rewritten to check that every `render_helpline_banner()` *call site* sits at 4-space top level, never inside a `with col:`. Both evals re-run after the fixes: still byte-identical to `368f083`. **Next: Phase D — corpus v2 (`docs/phases/10_corpus_rebuild_and_dense.md`).**
- **Phase D PLANNED 2026-09-16 — docs only, zero code, zero quota (`docs/phases/12_corpus_v2.md`, NEW):** a planning session that went looking for M3's premises and **falsified two of them**, which is why Phase D needed its own playbook instead of executing M3 of `10_corpus_rebuild_and_dense.md`. **Finding 1 — the Act gap was a SOURCING failure, not an OCR failure, and the published "no OCR can recover it" claim is WRONG.** The owner's untested lead downloaded to `6document.pdf` (14.1 MB) is *Official Gazette No. **10**, Vol. 106, 21 Jan 2019, Act No. 2, pages **A97–A122*** (the old playbook guessed "No. 11"): 27 pages, 0 embedded text chars, and — unlike the existing copy — **no duplicate adjacent page**. Targeted OCR (`rapidocr_onnxruntime`, dpi 200) of gazette pages 13/14/15 recovered `38.TheCommissionshall-` (A109), its `(e)…(r)` tail (A110) and `40.—(1) There shall be an Executive Secretary` (A111). **Then the sharper correction: both bodies were in the v1 processed text all along** — `(1)TheCommissionshall--` at **L614** (numeral `38.` OCR'd as `(1)`) and `(1) There shall be an Executive Secretary for the Commission who shall-` at **L545** (numeral dropped). The duplicate page 5–6 **is** real (adjacent-page cosine **0.978** vs a 0.794 runner-up, dpi 100 / 16×16 mean-pooled / **mean-centred** — centring is load-bearing, uncentred saturates at 1.000 for every pair and detects nothing) — it just did not cost cl.38 or cl.40. **`ACT_KNOWN_ABSENT = {38, 40}` (`scripts/audit_corpus.py:80`) is a false constant** currently excusing two clauses from the coverage gate; Phase D **deletes it, not empties it**. Scope limit stated in the playbook: only pages 1/13/14/15 of 27 were OCRed, so "all 58 clauses present" is a **D2 verification task, not a finding**. **Finding 2 — the Constitution needs only the cheap fix:** all **99** uncitable Constitution chunks are Arrangement-of-Sections material (88 under 60 words; all 11 at ≥60 words are *also* numbered title listings — `"236 Practice and procedure"`, `"89 Power as to matters of evidence"`), so **zero substantive body text is uncitable** and excluding the Arrangement pages takes `general` 4.7% → ≈0 and deletes the `toc-trap` class **without touching `CONST_SIZE = 400`**. M3's 2104 → ~500 section-unit re-extract — the riskiest change in the plan — **is not needed for the gate** and defers to Phase E. **Finding 3 — M3's order is backwards:** M2 ships a **per-chunk** embedding artifact keyed on a corpus sha256, so rebuilding the corpus after it invalidates every vector; **corpus first**, which is what `HANDOFF.md` already assumed by putting D before E without ever saying why. Also measured: the Act's **16 packed refs are all consecutive runs** (`cl. 3,4,5` … `cl. 56,57`) = pure 800-char cutting, mechanically fixable; the Factsheet's 19 are **disordered** (`Section 51,40`, `Section 50,45,54`) = `recursive_split(500/50)` cutting the S/N table mid-row. Playbook ships **D0–D6** with the refusal-floor recalibration **mandatory, not conditional** (dropping ~99 TOC chunks moves the IDF space and clause-aligned chunks run longer than 800; `MIN_SCORE` **may move down, may not move up**), `_is_toc_fragment` **kept and demoted to a lint assertion** rather than deleted, and the thing M3 never anticipated: **`conversations.json`'s 51 refs were verified against v1** and must be re-verified in D6. **Files: `docs/phases/12_corpus_v2.md` (new) + amendment boxes in `10_corpus_rebuild_and_dense.md` (M3 SUPERSEDED, M1/M2 reordered after D, cl.38/40 premise FALSIFIED — kept, not deleted) + `HANDOFF.md` + `STATUS.md` + `LEARNING_JOURNAL.md`.** No `src/`, no `scripts/`, no `data/` touched; `audit_corpus.py` still **AUDIT_CORPUS: PASS** (v1 tripwire, 9/9 ok) and `import app` still clean, re-run after the doc edits. **Next: execute Phase D on `phase10/corpus-v2`.** **Owner decision 2026-09-16: the 14 MB gazette PDF GETS COMMITTED** to `data/raw/` (consistent with the already-tracked 5.2 MB Act and 8.5 MB PLAC Constitution; never read at boot, so the slim Render runtime is untouched; accepted cost is a permanent large blob in history, so it lands in its own D1 commit). Still open: whether to push/PR the chat work before going **three unpushed branches deep**.
- **Phase D correction + design pass 2026-09-17 — docs only, zero code, zero quota:** the Phase D entry above is **partly wrong and the correction is load-bearing.** `4bcc763`'s Finding 1 claimed cl.38's body was in the v1 text at **L614**; **L614 is clause 48** (L612 reads `48.`; the Arrangement at L74 reads `48.Annual estimate and expenditure.` — the marginal note wrapped at L613/L615; v1 continues `(a) cause tobekept accounts and records` where the gazette continues `(a) formulate and implement policies`; `grep "formulate and implement"` returns **nothing** in v1). **cl.38 is genuinely ABSENT from v1** and is recovered from the **gazette** (A109–A110), not from v1; **cl.40's row was correct** (body at L545, bounded by `39.` L524 / `41.` L555). The match was made on string similarity **without reading the next line** — the same failure mode the 2026-09-16 journal entry was written to warn about, committed one day later. **The conclusion survives:** `ACT_KNOWN_ABSENT = {38, 40}` still retires (cl.40 never absent, cl.38 gazette-recoverable), all 58 clauses still reachable in v2 — but deletion moves to **D6**, after the re-OCR, not D2. **Three new findings from the re-check, all strengthening v2:** (1) **v1's cl.37 is silently corrupted** — a physical page is missing at the `===== PAGE 14 =====` boundary (L500); raw OCR p13 ends at cl.37(b), p14 opens mid-list at cl.38(j), so the duplicate p5/p6 **did** cost a page, just not cl.40's; (2) **a LIVE citation-integrity defect in `main`** — Act chunk 33 is reffed **`cl. 39`** and opens with cl.38's `(o)`–`(r)` incl. *"procure assistive devices for all disability types"*, so the app can serve cl.38's text under an `[Act cl. 39]` tag and `verify_citations()` passes it mechanically (chunk 31 `cl. 36,37` is clean, chunk 32 is `general`); (3) **v1 L542 reads `PARTVII`** where the Arrangement (L64) and gazette say **PART VIII**, and **v1's Arrangement truncates at L77 `51.Power to acquire land.`** — `audit_corpus.py:64-66` cites it as the source of `ACT_CLAUSES = range(1, 59)` but it lacks 52–58 (right number, wrong source), so **D2's Arrangement go/no-go gate must run on the gazette's front matter, which has not been OCRed yet** — now D2's first task rather than an assumption. **Also folded in: the D1+D2 implementation design**, so the next session executes rather than re-derives — `Chunk` gains a defaulted `path` field (**verified**: `defaults=` preserves 3-arg positional construction, so `rag.py:228,235,242` are untouched; the real risk is `len()` 3→4, `*chunk` splatting and 3-tuple unpacking, with a six-pattern `rg` sweep and the `bench_phase01.py:244,246,250` near-miss recorded); `build_corpus(version=None)` is a **pure relocation** of three blocks with **no memoization, no dict reordering** (insertion order breaks `PerDocRetriever` ties at `retrieve.py:331-338`) and **no `version=` on `ask()`**; a three-tier caller policy over the **14** call sites (never-flagged incl. `router.py:221`, whose `_retriever` global is **unkeyed**, and `rag.py:642`, which is `ask()`'s own fallback build; `--corpus=` equals-form-only; untouched quota-bound); and two prose claims become real gates — a `corpus_sha256()` fingerprint (`V1_EXPECTED`'s nine integers cannot see text **moving between chunks** at constant count) and an enforced `CE716FB3…5F19` assert **read back from disk**, because `write_text` emits CRLF on Windows and the published digest is of the CRLF bytes (**verified**: 594 CRLF pairs) — disclosed as Windows-specific and as adding one ` sha256=…` stdout line. D2 design: **three layers** so only `scripts/ocr_gazette.py` imports pymupdf/rapidocr (→ stdlib `parse_act_v2.py` → JSON-only `_act_chunks_v2()`), which is what keeps `requirements.txt` honest; **`scripts/ocr_local.py` is a trap — never import it** (calls `main()` at module scope, no `__main__` guard, unconditionally overwrites `data/processed/disability_act_2018_full.txt` at `:59`) → extract `ocrlib.py` **keeping `box` coordinates** (v1's assembler discards them) plus a guard and `--force`; **marginal notes are geometry-primary, title-as-validator** (measured on v1 OCR JSON p13: body `x0 107.4–363.6` vs notes `x0 1448.5–1466.2`; pure title-anchoring **fails on wrapped notes** — `Cessation of` / `office.` are separate OCR lines matching neither title alone — so group by y-adjacency first and detect the note side **per page**); **`dedupe_pages()` must NOT run on v2** (its salvage branch `load.py:347-363` appends unmatched lines onto the kept twin — a corruption mechanism on a clean source) and becomes an assertion that `detect_duplicate_pages(...) == []`; clause locator is a **monotonic cursor** (kills the `section 27 of the Interpretation Act` false-positive class structurally, no heuristic — and would have rejected the L614 match above) plus **two required anchors**, with `TITLE_WEAK`/`NUMERAL_MISSING` flagged never silent and no-anchor a hard failure; an **Arrangement go/no-go gate** with a written-in-advance fallback (titles from marginal notes, `"title_source"` recorded, discrepancies reported) and **never author a title**; **cross-check predictions written before the first run** (cl.38 absent · cl.37 large disagreement · cl.38(p)–(r) vs the `cl. 39` chunk · cl.40 agrees · PART VII/VIII disagrees; anything else is a parser bug) and **text-level only**, never page position, since the editions paginate differently; `ACT_V2_SIZE = 1100` as a **new** constant, explicitly **not** `ACT_SIZE` and **not tuned in D2** (longer chunks lower every cosine — that is D5's recalibration); `ref = "cl. %d" % n` from the parser so **packed refs are structurally impossible**; and the chunk header is the statutory heading (`38. Functions of the Commission.`) — the marginal note **restored to the position the printer moved it out of**, which makes `act_ref()` resolve v2 natively and the D2 validator a tight equality check. Playbook verification gains `git diff main -- data/processed/disability_act_2018_full.txt` **must stay EMPTY** — the playbook guarded `requirements.txt` but never the v1 TXT, the one artifact `ocr_local.py` can silently clobber. **Also recorded: `CLAUDE.md`'s "Act section-aware 800" is misleading** — `chunk.SECTION_RE` matches **4 times** in the entire cleaned Act text, all at 89.8%+ of the document (Second Schedule + Forms), so v1's Act split is effectively `recursive_split(text, 800)` for the whole operative body; not a bug (it is part of the byte-identical v1 path) but it means D2's clause-aligned chunking is a **total replacement** of the Act splitter, not a refinement. **Files: `docs/phases/12_corpus_v2.md` (Finding 1 corrected + Findings 1a/1b/1c + D1/D2 implementation notes) · `HANDOFF.md` (Finding 1 block + standing fact, itemised per clause) · `docs/phases/10_corpus_rebuild_and_dense.md` (amendment box) · `LEARNING_JOURNAL.md` (2026-09-16 entry boxed as half-wrong + new 2026-09-17 entry) · `STATUS.md` · `README.md` (stale since 2026-09-10 — status, chat usage, architecture, eval list) · `CLAUDE.md` (Phase D pointer + chat architecture + the §-aware caveat) · `AGENTS.md`.** No `src/`, no `scripts/`, no `data/` touched; `audit_corpus.py` still **AUDIT_CORPUS: PASS** (9/9 v1 tripwire ok), `eval_phase06.py` still hashes **`CE716FB3…5F19`**, `import app` still clean, `test_phase05.py` 137/137. **Fresh-eyes review: verdict ship-with-fixes, both fixes taken and disclosed in place** — it independently re-derived all six corrections and every design fact (all reproduce exactly) and found **two wrong line references in the new D1 notes**: the `bench_phase01.py` near-miss was cited as `:159,251,253` (those are `sum(...)` comprehensions and an `assert`; the real unpacks are **`:244,246,250`**), and the caller-policy table said **13** call sites while omitting **`src/rag.py:642`** — `ask()`'s own fallback `PerDocRetriever(build_corpus())`, i.e. **the user path's default corpus build**, the single caller the table most needed — plus `app.py:457`/`router.py:220` cited the `from rag import …` lines rather than the calls at `:458`/`:221`. Corrected to **14** with dated in-place notes rather than silently, in a session whose entire subject is unverified references. **Next: execute Phase D on `phase10/corpus-v2`, starting with the gazette Arrangement OCR + go/no-go gate.**
- **Phase 10 A/B/C SHIPPED 2026-09-17 — PR #12 merged, LIVE on Render:** the four-deep branch stack and 11 unpushed commits are gone; `main` moved `bb6f931` → `6b10f62`. **First deploy where users get the multi-turn chatbot instead of the single-turn form.** Suite re-run green before push (137 / 51 / 16+7 / 10 / audit 9-9 / `import app`). The orphaned 2026-09-17 doc pass (740 lines, incl. the L614 correction) was committed first — it would not have survived a `git checkout`. Both open owner decisions in `HANDOFF.md` are now closed: push+PR first, then branch off a merged `main`.
- **Phase D — D0+D1 DONE, D2 GATE ANSWERED 2026-09-17 (zero Gemini calls, branch `phase10/corpus-v2`):** **THE GATE IS GO — the gazette's Arrangement yields a clean `1..58`, no holes, no duplicates, nothing out of range.** D2's manifest-anchored parser is unblocked, the written-in-advance marginal-note fallback was **not needed and not used**, and **no title was authored**. Manifest on disk: `data/processed/gazette_arrangement.json` (58 entries, `"title_source": "arrangement"`) — do not re-derive it. Pages 2–4 (A97–A99) at dpi 200, conf 0.978–0.984; Finding 1c was right that v1 could not settle this (v1's Arrangement truncates at L77 because it lost the page carrying 52–58 — that page is present and legible in the gazette). **D0:** gazette filed at `data/raw/disability_act_2018_gazette_FGP.pdf`, **verified from its cover page before filing** (Gazette No. 10, Vol. 106, 21 Jan 2019, Act No. 2, A97–A122, FGP 007/12019/700; 27 pages, no text layer), own commit as the owner decided. **D1:** `Chunk` gains defaulted `path` (all three 3-arg sites untouched as predicted; the `bench_phase01.py:244-250` near-miss confirmed harmless and left alone); `build_corpus(version=None)` is a **pure relocation** — the three loading bodies do not appear in `git diff -w` at all; `--corpus=` equals-form on 6 harnesses, 5 left bare, 3 quota-bound files not opened. **Two prose claims became gates, and both were NEGATIVE-TESTED** — `corpus_sha256()` pinned at `25650238…e89a` catches one character moving *between* adjacent chunks at constant count (all nine shape integers stay identical — the exact blind spot) and catches a docs-dict reorder (which moves rankings via `PerDocRetriever`'s insertion-order tie-break); `eval_phase06` asserts `ce716fb3…5f19`, **Windows/CRLF-specific by construction** (594 CRLF pairs, read back from disk as bytes) and says so on failure instead of raising bare. Stdout **re-baselined, not broken silently**: +2 lines in `audit_corpus`, one ` sha256=…` in `eval_phase06`; everything else byte-identical to pre-edit baselines. **THE 23-OF-58 NEAR-MISS, worth remembering:** the first parse read 23 entries and looked exactly like a truncated source — it was not. RapidOCR emits the clause number and its title as **separate boxes** whose vertical centres differ by a few pixels, so the title often sorts *before its own number*; a text-only parse cannot see it. Row-grouping by geometric overlap recovers all 58. **Same class of failure as v1's 25/62 uncitable Act chunks**, and the concrete vindication of keeping the geometry v1's assembler discarded. Reported-not-patched: cl.26 parses as `"S Service at queues."` (OCR doubled the leading letter). **cl.38 caution: its TITLE is in the Arrangement, which says nothing about its BODY** — `ACT_KNOWN_ABSENT` untouched, D6 still owns deleting it. **LIVE HAZARD FIXED:** `scripts/ocr_local.py` had a bare `main()` at module scope with no `__main__` guard and `main()` unconditionally overwrites the v1 Act corpus at `:59` — **`import ocr_local` destroyed it silently**; guard + `--force` added, both verified (import inert, `main()` refuses, sha256 unchanged). Helpers extracted to `scripts/ocrlib.py` **keeping the boxes**, with function-local pymupdf/rapidocr imports so importing it pulls in no ONNX; `assemble_v1()` preserved verbatim. `dedupe_pages()` **not run** — `detect_duplicate_pages() == []` asserted instead, scoped to the 3 front-matter pages and the script says so. Session end: all five harnesses byte-identical, `eval_phase06` digest passes with reverse_rel 0.630 **preserved untuned**, bench/03/04/05/09 green, `import app` clean, **`requirements.txt` and the v1 Act TXT both show an EMPTY diff vs `main`**. **Next: D2's clause locator + the 27-page body OCR.**
- **Phase D — D2 body OCR + clause locator + cross-check DONE 2026-09-17 (zero Gemini calls, branch `phase10/corpus-v2`):** **58/58 clauses located on BOTH required anchors — zero `TITLE_WEAK`, zero `NUMERAL_MISSING`, no title authored.** Manifest at `data/processed/act2018_v2_clauses.json`. Body OCR 27/27 pages, dpi 200, 340 s, conf 0.967–0.981, **zero low-confidence lines**; pages 2–4 seeded from the front-matter checkpoint so the Arrangement bytes are the exact bytes the gate answered GO on. **The deferred duplicate check is discharged at full scope:** `detect_duplicate_pages()` over all 27 pages finds **none** (top adjacent pair **0.219** vs v1's **0.978** outlier); `dedupe_pages()` not run. **CROSS-CHECK: all three written-in-advance predictions hold** — cl.37 LARGE (0.50), cl.38 LARGE→partial (0.61), cl.40 agrees (0.90); **52/58 agree**; and the decisive probe *"formulate and implement policies"* is **absent from v1 and present in v2 cl.38 — clause 38's opening is RECOVERED by the gazette.** **NEW FINDING — cl.53 is a second v1 truncation of the cl.38 class:** *"awarded against the Commission"* is absent from v1 outright and v1's text at `53.` reads `53. | judgment debt. | shall bepaidfrom theFund of theCommission.` (marginal note spliced in, body cut); decided on **substring presence, not a ratio**. cl.20/27/44 adjudicated as **OCR divergence, not missing text** (distinctive probes resolve in v1) and **not claimed as recoveries** — adjudications live in a dict kept **separate from the predictions**, because predicted-in-advance and explained-afterwards are different evidence. **THE YARDSTICK WAS WRONG FIRST AND WAS FIXED BEFORE ANYTHING WAS READ INTO IT:** at k=40 only **7/58** clauses could agree at all (independent OCR noise in both editions destroys every shingle spanning a bad character), so k was **calibrated against clauses whose answer Finding 1 already settled** — k=10 separates cl.40 (0.90) from cl.37/38 (0.50/0.61) where k≥18 does not; a second, uncalibrated local-alignment measure scored cl.40 at 0.18 and was **discarded rather than used to adjudicate the calibrated one**. **THREE GEOMETRY FAILURES, each producing a plausible wrong answer rather than an error:** (1) splitting columns on the widest **gap** reintroduced the verso/recto asymmetry the design existed to avoid — recto ~418px fired, verso ~154px did not, so verso notes stayed in the body and `rows()` welded them on (`"Accessibility 5. Road side-walks…"`), unmatchable by any start-anchored numeral regex — **first run 16/58**; (2) body extent as **min/max** over wide lines broke on single outliers (p11 one merged OCR box swallowed all 13 notes; p13 the margin clipped `"Functions of"`/`"Commission."` — **cl.38's own title** — into the body); (3) `FURNITURE_RE` under global `IGNORECASE` matched **`"Participation"`** as `PART`+`[IVX]` and deleted cl.30's note, leaving it to score 0.61 and flag `TITLE_WEAK`. Notes are matched by a **window over consecutive lines**, not a y-gap threshold: within a wrapped note the gap is **−2 px** but two *different* notes on p13 sit **28 px** apart. Clause text stops at the First Schedule — without that bound `58. Citation.` absorbed **5,480 chars** of Schedule text. **Guards added because the obvious next command was destructive:** `--pages=1-27` would have overwritten the verified 58-entry Arrangement manifest with a body-page parse → `--ocr-only` plus a refusal to replace a **clean** manifest with a dirty one. `rows()` extracted to `ocrlib` for the second caller; gate re-run after the move, still 58/58, manifest bytes unchanged. New: `scripts/parse_act_v2.py` (**stdlib only** — the JSON seam that keeps the slim runtime honest), `scripts/crosscheck_act_v2.py`. Verification: `audit_corpus`/`eval_heldout`/`eval_chat`/`ablate_phase08` all **byte-identical to baseline (0 diff lines)**, `eval_phase06` still `ce716fb3…5f19`, bench PASS, 03/04/05(137)/09(51) green, `import app` clean, **`requirements.txt` and the v1 Act TXT both EMPTY diff vs `main`** (TXT sha256 `940657A48C69B4E4…` unchanged). **Next: `_act_chunks_v2()` + `ACT_V2_SIZE = 1100`** — the only thing between the parse and D2's exit criteria (`general` ≤1%, `packed` 0, 58/58 citable). Do not re-derive the manifest and do not re-run the OCR.
- **Phase D — D2 DONE 2026-09-18 (`_act_chunks_v2()`; zero Gemini calls, branch `phase10/corpus-v2`):** **the Act half of corpus v2 exists and clears its gate — `general` 0 (0.0%), `packed` 0, 58/58 citable, 65 chunks, 0 over cap, lengths 117/410/1100.** v1 carried **25 of 62 uncitable + 16 packed**; that class — named three separate times before this phase (Act cl.19, `CT7.t2`, the 25/62 measurement) — is **closed on the Act**, and `ref = "cl. %d" % n` from the parser makes packed refs **structurally impossible**, not merely absent. `audit_corpus.py --corpus=v2` **still exits 1 and that is correct**: the Constitution (99 general) and Factsheet (9 general/19 packed) rows are **untouched v1 numbers** owned by D3/D4 — nothing should expect exit 0 until D4. **Pinned decisions held, none re-litigated:** `ACT_V2_SIZE = 1100` is a **new** constant (`ACT_SIZE` untouched at 800) and **was not tuned** (D5 owns recalibration); `_act_chunks_v2()` reads **only** the manifest JSON via stdlib `json`, preserving the three-layer seam that keeps `requirements.txt` slim; every chunk carries `path="manifest"`; `CORPUS_VERSION` stays `"v1"` and **the user path cannot reach v2** (`build_corpus("v2")` only via `--corpus=`; `ask()` has no `version=`, `router._retriever`/`app.py` untouched). Only **two** clauses split: cl.38→3, cl.57 (`Interpretation.`, 5,311 chars)→6; cl.57 is the definitions section, **not Schedule bleed** (the First Schedule bound holds; cl.58 is a clean 103 chars). **Two disclosed deviations, both accepted:** (1) the header is **budgeted out of** the cap (`ACT_V2_SIZE - len(header) - 1`, floor 100) rather than added on top — prefixing a header to every sub-chunk of an 1100-char split otherwise produces chunks **over** the cap; mirrors `constitution_aware_split._emit()`'s `§N` handling (`src/chunk.py:125-132`), `ACT_V2_SIZE` itself untouched, and verified by reconstruction that **no text is dropped** (every 50-char window of each clause body survives; surplus is exactly `recursive_split`'s overlap; the floor-100 branch is unreachable at a 84-char max header); (2) `SIZE_CAP` became `size_caps(version)` with `caps` passed explicitly instead of a global `main()` mutates — **no external importer** of it, `audit_doc` or `length_histogram`. **THE HEADLINE NUMBER WAS TALKED DOWN, NOT UP:** the `act_ref()` validator reports **65/65 = 100%** and the review established it is **close to a TAUTOLOGY** — the parser writes `header = "%d. %s" % (n, title)` from the same `n` it builds `ref` from, `_act_chunks_v2()` prefixes that header to every sub-chunk, and `act_ref()` recovers the numeral from that very string; the sources differ in **inference, not upstream**, so this is **not** the `assert_frozen10_matches_notebook()` discipline `audit_corpus.py`'s docstring had claimed. It still catches a stray line-start `NN.` flipping `act_ref()` to a member list, and header/ref drift from future chunking — **narrow but real**. The limit is now printed **next to the number**, the module docstring is **amended in place**, and **D6 must re-scope the check before asserting on it** (validate against the Arrangement *title*, which does not share the numeral's upstream, or scope the assert to those two cases). **New `act_manifest_flags()` report** added on the same review finding: `_act_chunks_v2()` never inspects `clause["flags"]`, so a manifest regenerated with `TITLE_WEAK`/`NUMERAL_MISSING` would be promoted to fully-citable chunks with **no signal anywhere**, while D6 deletes `ACT_KNOWN_ABSENT` on that manifest's word; reads `located 58/58 · missing none · degradation flags none` today. Verification: `audit_corpus` v1 stdout **0 diff lines against the pristine stashed code** (stronger than a saved baseline), `eval_phase06` still `ce716fb3…5f19`, v1 `corpus_sha256` still `25650238…e89a`, 9/9 tripwire ok, `eval_heldout`/`eval_chat`/`ablate_phase08` exit 0 unchanged, bench PASS, 03 16/16+7/7, 04 10/10, 05 **137**, 09 **51**, `import app` clean, **both git guards EMPTY**. **Fresh-eyes review: verdict ship** — it reproduced every harness independently, checked all 65 chunks against the manifest by hand, confirmed insertion order in both `build_corpus` branches and that v2 is unreachable from the user path; its one substantive finding (the circularity) was taken. **Next: D3 — Factsheet S/N table** (its **19 packed refs are DISORDERED**, `Section 51,40` — `recursive_split(500/50)` cutting the table mid-row, **a different defect from the Act's consecutive runs, so the Act's fix does not transfer**), then D4 → D5 → D6.
- **Doc pass verified + committed 2026-09-18 (docs only, zero code, zero quota):** the D2 session-close pass (HANDOFF banner · `12_corpus_v2.md` results + "next session starts here" · `LEARNING_JOURNAL.md` 2026-09-17-later-still · this file) was sitting **uncommitted** and is now on `phase10/corpus-v2`. Claims re-verified against disk before committing rather than taken on trust: `act2018_v2_clauses.json` holds **58 clauses with 0 flagged**, all three D2 artifacts exist (`gazette_arrangement.json` 2.7 KB · `act2018_v2_clauses.json` 42 KB · `gazette_rapidocr.json` 277 KB), and **both git guards re-run EMPTY** (`requirements.txt`, v1 Act TXT). `_act_chunks_v2()` confirmed **absent from `src/rag.py`** — "next" is genuinely next, not already half-done. **Two stale references found and fixed:** (1) `ACT_KNOWN_ABSENT` is cited as `scripts/audit_corpus.py:80` in four forward-looking places but D1's comment block moved it to **`:90`** — corrected in `HANDOFF.md` ×2, `12_corpus_v2.md`, `10_corpus_rebuild_and_dense.md`, with the old number kept in a dated note; **`LEARNING_JOURNAL.md` left alone on purpose** (dated entries were correct when written — the journal is a log, not a pointer). (2) the stray 0-byte `D:NGORAG_review_judge.diff`, carried as open housekeeping since Phase 06, is **gone** (`git status -uall` clean, root glob matches nothing) — both mentions struck through as RESOLVED rather than deleted. **Next is unchanged: `_act_chunks_v2()` + `ACT_V2_SIZE = 1100`.**
- **Phase 10 B review addendum 2026-09-15:** fresh-eyes review re-derived every quoted number from a clean run (byte-identical to the committed baseline) — all match. One real defect found: **`CT7.t2` is UNSCOREABLE, not missed** — it expects `act2018:[5]` but the chunk carrying the Act First Schedule list is reffed **`general`** (`ref_nums("general")` is empty), so both arms retrieve that exact chunk at ranks 5-6 and still score 0.000. `chatset.verify_expected()` passed it because an unrelated `cl. 3,4,5` chunk carries a 5 — it proves the *number* exists, not that the right *text* is reachable under it; that limit is now documented in the function, together with a second one (Constitution section numbers **collide across chapters** — s.36/18/34/40/42 — so `(doc_id, number)` matching can produce a false POSITIVE; verified not live in either set today). **The expectation was NOT edited** — the correction would have moved chat_test's pronoun class *in our favour*, i.e. the exact class recorded as "did not improve"; a dated addendum went into the turn's `note` instead. Net: a **second named instance** of the uncitable-chunk class (alongside "Act cl.19 is uncitable" and the 25/62 uncitable Act chunks) and one more piece of evidence for **Phase D**, whose audit gate is `ref=="general"` ≤1% per doc.
- **Phase D — D3 DONE 2026-09-18 (`_fact_chunks_v2()`; zero Gemini calls, branch `phase10/corpus-v2`):** **the Factsheet is row-aligned — 32 chunks · `general` 0 (0.0%) · `row_spanning` 0 · 0 over cap (900) · 27 rows, S/N 1–27 contiguous, zero degradation flags.** v1 carried **48 chunks, 9 uncitable (18.8%), 19 packed and 26 of 48 chunks cut ACROSS table rows.** The 9 `general` chunks were checked individually, not assumed: **7 are excluded by region** (cover page, intro prose, *Arrangement of Sections* ×2, PLAC address/About/Supported-by ×3) and **2 are absorbed and made citable** — chunk 28 is row 17's tail (*"at least 5% … in their employment- section"*, numeral lost to a page cut) and chunk 36 is page furniture welded onto row 22's continuation; both now sit inside `Section 28` / `Section 38`. **THE TARGET WAS UNREACHABLE AND MEASUREMENT SAID SO BEFORE ANYTHING WAS WRITTEN.** The playbook asked for `packed 0`; **`packed 0` and `evalset.verify_expected()` cannot both hold.** Eight sections — **11, 13, 15, 23, 34, 35, 46, 53** — are **never row anchors**; they exist only as cross-references inside another row's provisions (*"…may also accept a gift of land, money or property… - section 46"* lives in the row headed `Section 45`). `verify_expected()` is a **HARD assert** — *"an expected number that no chunk carries is a ground-truth bug"* — and **frozen10/Q6 expects 11**, with five held-out and four test questions expecting the rest. An anchor-only ref would have **deleted those eight from the corpus and crashed** `eval_heldout`/`eval_phase06`/`audit_corpus`; the only escapes were editing **frozen10** (forbidden outright) or deleting held-out/test expectations — i.e. moving the yardstick. Measured both ways: anchor-only gap `[11,13,15,23,34,35,46,53]`, anchor+cross-reference gap `[]`. So the ref carries the **anchor first, cross-references ascending** (v1's were **disordered** — `Section 51,40` — which was the tell that this was table damage, not over-run), and `packed` reads **16**: a **declared non-defect**, because it is the table's own content. **`V2_MAX_PACKED` was NOT touched and the collision was left VISIBLE** — `--corpus=v2` still prints `factsheet2020 packed refs 16 <= 0: FAIL` with the reason printed underneath, and **D6** now carries a boxed instruction to gate this doc on `row_spanning == 0` instead. A threshold quietly widened to fit a measurement reads, three commits later, exactly like one that was always right. **The replacement metric has DEMONSTRATED discriminating power, not assumed:** `row_spanning` re-scans each **emitted chunk's TEXT** for the S/N row-start line — never *"we emit one row per chunk, therefore 0"*, which would restate the code and gate nothing — and **v1 scores 26/48 against v2's 0/32**. Verified rather than assumed that a v2 header (`Section 45 Funds of the Commission`) carries no leading S/N numeral and so cannot match the regex; had it matched, the fix would have been the header, not an exemption. **NO MANIFEST, on purpose:** D2's JSON seam exists because `pymupdf`/`rapidocr` must never reach `requirements.txt`; the factsheet's source is already a clean TXT the repo parses at boot with stdlib, so `fact_v2_rows()`/`_fact_chunks_v2()` are in-process, stdlib `re` only — symmetry is not a reason to add an artifact that must be kept in sync forever. The D2 *idiom* did carry over intact (refs from structure, per-row degradation flags the audit prints, header budgeted out of the cap, `path` recording how the ref was obtained — here `"table"`). **Implementation, one production file (`src/rag.py`) + reporting in `audit_corpus.py`:** `FACT_V2_SIZE = 900` is a **new** constant (`FACT_SIZE`/`FACT_OVERLAP` untouched at 500/50 — the byte-identical v1 path) and **was not tuned** against the refusal floor (D5 owns that); cap sweep recorded in code (700→39 chunks, 900→32, 1200→28, 1800→27, **0 over cap at every one**). Region = `[first row anchor, first tail marker)`, excluding **1,880 chars** of cover/intro/Arrangement at the head and **894 chars** of PLAC matter at the tail; page furniture dropped **inside** rows too (rows 5, 15, 22, 25 each span a page break). **Every sub-chunk carries the full row ref and header** — per-piece cross-references were considered and rejected, since they make coverage depend on where `recursive_split` happens to cut and a cut through the literal string `section 46` would silently drop a frozen expectation. **`fact_ref()` is NOT promoted to a v2 validator** (unlike `act_ref()`) and its docstring now says why: under the full-row-ref rule a sub-chunk legitimately names sections its own text does not contain, so disagreement is the design and a "validator" would measure the split point — D2's lesson applied **before** the fact. **Predicted 13 packed, measured 16 — recorded, not reconciled by a code change** (13 counts rows under a wider cross-reference regex; 16 counts chunks, 5 of the 11 multi-number rows splitting in two). D3 reuses **exactly** v1's two regexes, so v2 changes which *text* a number attaches to and never the vocabulary; the accepted cost is that *"sections 4 and 5"* / *"sections 26 and 27"* stay unmatched, as in v1, and no eval expects them. **Verification:** `evalset.verify_expected()` passes against a **v2** docs dict (frozen10 39 pairs / heldout 48 / test 34 / all 121) in the real harness, and the factsheet numbers reachable from some `ref` are **identical in v1 and v2** — the same 43 — so nothing was lost by the rebuild and nothing invented. Reconstruction check (D2's): **0** rows with a dropped 50-char window and **0** in-region source lines missing from any row. v1 `corpus_sha256` still **`25650238…e89a`**, `eval_phase06` still **`ce716fb3…5f19`**, 9/9 tripwire ok; `eval_heldout`/`eval_chat`/`ablate_phase08`/`bench_phase01`/`test_phase03`/`test_phase09_ops` stdout **0 diff lines vs the pristine stashed code**, `test_phase04` 2 lines (wall-clock latency) and `test_phase05` 4 lines (timestamped Streamlit `missing ScriptRunContext!`) — both benign and both predicted; bench PASS · 03 16/16+7/7 · 04 10/10 · 05 **137** · 09 **51** · `import app` clean · `requirements.txt`, v1 Act TXT and v1 factsheet TXT all **EMPTY** diff vs `main`. **DISCLOSED: `audit_corpus.py`'s v1 stdout is re-baselined, not byte-identical** — **13 diff lines**, every one the new `row-span` column (PER DOC header + 3 data rows + 5 legend lines), **every pre-existing number unchanged** (62/25/16, 2104/99/0, 48/9/19, caps, lengths). Unavoidable: printing the metric for **both** versions is what makes v1's 26/48 available as proof it can fail, and the session plan's "0 diff lines" and its anti-tautology requirement are mutually exclusive — the metric won. `--corpus=v2` **still exits 1, now on TWO rows**: the Constitution's 99 `general` (D4's) **and** the factsheet's `packed 16 <= 0` (D6's to re-scope). **Next: D4 — Constitution Arrangement exclusion** (keep `CONST_SIZE = 400`, keep `_is_toc_fragment` as a **lint assertion**, do not delete it), then D5 (refusal floor, mandatory) → D6 (re-baseline + the factsheet gate re-scope).
- **Phase D — D4 DONE 2026-09-18 (`_const_chunks_v2()`; zero Gemini calls, branch `phase10/corpus-v2`):** **the Constitution's Arrangement of Sections is excluded and ALL THREE DOCS ARE NOW v2 — 2037 chunks · `general` 34 (1.67%) · `toc_general` OUTSIDE Chapter VIII **0** · inventory **8 unnumbered + 26 Chapter VIII** · 0 over cap (400).** v1 carried **2104 chunks, 99 uncitable (4.71%), 7 `toc_general` outside Ch VIII**. **THE TARGET WAS WRONG FOR THE SECOND PHASE RUNNING AND MEASUREMENT SAID SO FIRST.** Finding 2 promised `general` **→ ≈0**; the measured floor for Arrangement exclusion is **34 (1.67%)**, so `V2_MAX_GENERAL_PCT = 1.0` is **unreachable on this document**. The residual 34 is two classes, **neither a defect**: **8 `general_unnumbered`** (the Preamble ×2 and six chapter-divider headings — no section number exists, so any `s. N` would be fabrication) and **26 `general_toc`**, **all 26 in Chapter VIII** (Second/Third/Seventh Schedule legislative-list items `8. Census` / `63. Traffic`, plus the Fundamental Rights (Enforcement Procedure) Rules `ORDER 6` / `FORM NO. 4` / dotted-rule boilerplate) where `general` is the **correct** ref, because a Schedule **item** number is not a **section** number and labelling item 8 as `s. 8` re-creates the Q10 misattribution class on purpose. **Deleting the Schedules to pass the gate is forbidden** — the Second Schedule is operative law and the Rules are the mechanism a PWD uses to enforce Chapter IV; the honest fix is a citable `Sch. N item M` ref, i.e. a **fourth numbering scheme** through `cite_tag()`/the citation invariant/the prompt, which is **Phase E**. **`V2_MAX_GENERAL_PCT` was NOT touched** — `--corpus=v2` still prints `constitution1999 uncitable 1.7% <= 1.0%: FAIL` with the reason underneath and **still exits 1**, now on the Constitution *and* the factsheet's `packed 16`; **D6 owns both**, exactly as D3 left its collision visible. **THE NAIVE CUT POINT WOULD HAVE SILENTLY EATEN THE PREAMBLE.** Chapters I–VIII appear **twice** in `constitution_1999_NHRC.txt`; the obvious cut is the operative body's second `Chapter I` (char 18,535), and the real Preamble (*"We the people of the Federal Republic of Nigeria … Do hereby make, enact and give to ourselves the following Constitution:-"*, char **17,917**, 616 chars) sits **between** the two runs. Cut@2nd-Chapter-I yields a **better-looking** number (2035 chunks / 32 general) bought by deleting the enacting words of the instrument; cut@Preamble yields 2037 / 34. It only showed up because the boundary was eyeballed rather than assumed. **`CONST_PREAMBLE_RE` is asserted to match exactly once** and the cut asserted to separate the two chapter runs (last heading before it `VIII`, first after it `I`) — D2/D3's flag discipline, because a silent fallback to chunking the whole document would rebuild v1 under a v2 label and the gate would "pass" for the wrong reason. **D4 CURES THE Q10 CLASS AT SOURCE.** The re-scoped metric `toc_general` is measured from each **emitted chunk's TEXT** (does it carry a `§N` prefix inside `const_ref()`'s own 160-char window that `_is_toc_fragment` then demoted?), never from construction — *"we excluded the Arrangement, therefore 0"* would restate the code and gate nothing. **v1 scores 7 outside Chapter VIII, v2 scores 0**, and the **7th v1 chunk is the Q10 trap itself**: `Chapter IV - Fundamental Rights §39: "ion from fundamental human rights. 46 Special jurisdiction of High Court and Legal aid."`. That chunk now **ceases to exist**, where `_is_toc_fragment` only ever *mitigated* it by relabelling — so **the heuristic is KEPT** (its docstring records the inversion) and survives as the **instrument that proves the cure held**. **Stated limitation, not papered over:** "in the Schedules" is **proxied** by the `Chapter VIII` label `constitution_aware_split` prepends, and Chapter VIII also holds operative sections 297–320, so a genuine Ch VIII body chunk wrongly demoted would hide in the in-Ch-VIII bucket — the **pinned 8 + 26 inventory** is what covers that hole. **The change is narrower than it sounds: 2035 of v2's 2037 chunks are byte-identical in `(ref, text)` to v1's last 2035.** Only the Preamble differs — in v1 it sat *inside* the Arrangement's Chapter VIII block and was labelled `Constitution, Chapter VIII - Federal Capital Territory…` (3 chunks); in v2 it precedes the first chapter heading, `constitution_aware_split` labels it `Constitution, Preamble:`, and the shorter prefix fits the same 616 chars in **2** chunks. **`CONST_SIZE` does not move and there is deliberately NO `CONST_V2_SIZE`** — a new constant would imply the size moved; D4 replaces the **region**, running v1's own `clean_text(repair_joins(raw))` pipeline, v1's splitter and v1's `const_ref()` with **no ref-shape change** (unlike D3). `_const_chunks_v1()`/`const_ref()` untouched; `build_corpus()`'s v2 branch gains one line with **insertion order unchanged** (`src/retrieve.py:331-338` breaks score ties by it). **The excluded region carries no operative text, measured rather than asserted:** `text[:17917]` is **3.44%** of the document and **690 non-empty lines**, of which **zero** contain the word `shall`, the longest carries **14** words and only **one** exceeds 12 — title listing end to end. **Coverage — the D3 trap did not fire, checked first:** sections reachable from a Constitution ref are **318 before and 318 after, set difference empty in BOTH directions**, all 17 evalset-expected numbers (`6,16,17,18,34,35,36,40,42,45,46,65,66,77,85,117,251`) still reachable, and `evalset.verify_expected()` passes against a **v2** docs dict in the real harness (frozen10 39 pairs / heldout 48 / test 34 / all 121). **The 2104 → ~500 section-unit re-extract stays DEFERRED to Phase E** — not needed for the gate and the riskiest change in the original plan. **Verification:** v1 `corpus_sha256` still **`25650238…e89a`**, `eval_phase06` still **`ce716fb3…5f19`**, 9/9 tripwire ok; `eval_heldout`/`eval_chat`/`ablate_phase08`/`bench_phase01`/`test_phase03`/`test_phase09_ops` stdout **0 diff lines vs the pristine stashed code**, `test_phase04` 2 lines (wall-clock latency) and `test_phase05` 4 lines (timestamped Streamlit `missing ScriptRunContext!`) — benign and predicted; bench PASS · 03 16/16+7/7 · 04 10/10 · 05 **137** · 09 **51** · `import app` clean · `requirements.txt` and all three v1 processed TXT files **EMPTY** diff vs `main`. **DISCLOSED, exactly as at D3: `audit_corpus.py`'s v1 stdout is RE-BASELINED, not byte-identical** — **37 diff entries (33 `=>`, 4 `<=`)**: the PER DOC header + 3 data rows gain a `toc-gen` column, plus 8 legend lines and a 21-line `CONSTITUTION general INVENTORY` section printed for **both** versions so v1's 7 is available as proof the metric can fail. **Every pre-existing number unchanged** (62/25/16, 2104/99/0, 48/9/19, row-span 0/0/26, caps, lengths). **Recorded, not fixed here:** `eval_heldout --corpus=v2` and `ablate_phase08 --corpus=v2` exit 1 (frozen-10 recall **0.666** vs the pinned v1 **0.925**) — **this pre-dates D4**, re-verified identical by re-running both at `6a5f5d9`; D5 (refusal floor) and D6 (re-baseline) own it and `eval_heldout.py` fails on the *frozen* yardstick by design. Direction of travel is good and **nothing was tuned against it**: held-out **0.420 → 0.473**, test **0.338 → 0.471**, `eval_chat --corpus=v2` answers **19/23** where D3's v2 answered 18/23 (one fewer refusal). **Next: D5 — the refusal floor, MANDATORY** (re-derive `src/retrieve.py:26-44` against v2; `MIN_SCORE` may move **down**, never up; gate `false_refusal_v2 ≤ false_refusal_v1`), then **D6** (flip `CORPUS_VERSION`, re-scope all three gates, delete `ACT_KNOWN_ABSENT`, re-verify `conversations.json`'s 51 refs).
- **Phase D — D5 DONE 2026-09-19 (`scripts/calibrate_refusal.py`; zero Gemini calls, branch `phase10/corpus-v2`):** **the refusal floor was RE-DERIVED against v2 and `MIN_SCORE` DID NOT MOVE — it stays `0.10`.** D5 was **mandatory, not conditional** (D2/D3/D4 replaced all three documents, so every cosine in the system moved and the 2026-09-08 calibration was inherited rather than re-derived), and it turned out to need **no parameter change at all** — measurement discharged it, and that is a result, not a non-event. **The hazard D5 exists to catch did not materialise:** false refusals are **0/10 · 0/25 · 0/17** on frozen10/dev/test under **both** corpora, and v2 is marginally *better* on the false-answer side (**H25 0.1032 → refused**, one fewer off-corpus dev row clearing the floor; test unchanged at 3/3). **The in-corpus/off-corpus band is STILL INVERTED and that conclusion was re-measured, not inherited** — v1 weakest in-corpus **0.1285** (T1) vs strongest off-corpus **0.4193** (H23); v2 **0.1209** (T9) vs **0.4195** (H23) — so no value above 0.10 would buy semantic discrimination anyway; that remains the strict-prompt `NO_ANSWER_SENTENCE` layer's job. In-corpus minima moved **up** on the frozen set (0.1696 Q6 → 0.2287 Q10) and were flat on dev (0.1359 → 0.1369, H11 both). **The playbook's escape hatch — a second frozen v1 index used only for the answer/refuse decision — is DECLINED**, because its stated trigger ("only if a false refusal appears") never fired across **52 answerable rows × 2 corpora**; two indexes is real cost and there is nothing to buy with it. **THE COMMITTED BATTERY THE SOURCE ASKED FOR BY NAME NOW EXISTS.** `src/retrieve.py` had said since 2026-09-13 that its refusal-invariance proof rested on "a run nobody can reproduce" — a throwaway harness — and that "Phase 10 D needs this battery as a committed script". `scripts/calibrate_refusal.py` is it: **106 probes** (60 `evalset.load_eval_set()` rows + 12 `ablate_phase08.OFF_CORPUS` + 34 `retrieve.SYNONYMS` keys, **all imported, none copied**) × **both corpus versions built in ONE process** on the shipping arm (`K_PER_DOC=3`/`TOP_N=6`/`MIN_PER_DOC=1` reused from `eval_heldout`, whose import was verified inert), zero network, **writes no files** — **212 probe-runs, 0 disagreements.** **All three gates are negative-tested BY INJECTION, not by reading** (`--negative-test`), and **one result is reported honestly rather than glossed: gate 1's PRESCRIBED injection cannot falsify it.** Passing `floor=0.0` to `select_top` while still filtering at `MIN_SCORE` — the "latent inconsistency" its own docstring describes — leaves invariance **intact**, because the global maximum is in the output at *every* floor (at 0.0 the quota phase takes each doc's best hit, and the global max **is** some doc's best hit; at `MIN_SCORE` it is either taken by the quota phase or taken first by the fill phase). The inconsistency is real but changes *which six chunks are shown*, not the refusal decision. Gate 1 is proven live instead by a **mutant selector that drops the global max** (2 disagreements, fires). Gate 2 fires under a **deliberately ASYMMETRIC** injection (v2's floor → 0.30, v1 left alone — raising both arms together could push them under as a pair and still satisfy `v2 ≤ v1`, proving nothing), naming **38 offenders**; gate 3 fires at `MIN_SCORE` → 0.11. **Gate 3 is the one thing in D5 that constrains a future session: the floor MAY MOVE DOWN ON EVIDENCE AND MAY NEVER MOVE UP**, now machine-enforced rather than only written down. **FROZEN-10 ATTRIBUTION, the bounded diagnosis** (`eval_heldout.py` tables dev and test only, so the move was previously unattributable): the **0.925 → 0.666** plain-recall drop is the **packed-ref subsidy being withdrawn, not a ranking regression** — packed chunks retrieved across the 10 questions fall **14 → 5**, all **12** lost expected refs sit in the 7 questions that had them (**Q2 alone loses `act 5,6,7` + `fact 6,7`**, which v1 satisfied from three packed chunks), **zero refs were gained**, `recall_strict` — which never granted that subsidy — moves only **0.701 → 0.633** and is *flat or better* on **6 of 10** (Q8 **0.500 → 0.750**), and top scores largely **rise** (Q6 0.1696 → 0.2459, Q8 0.3049 → 0.5416). **Nothing was tuned on this**; the block is marked report-only in the script for the contamination reason `evalset.py` exists. **THE FITTED SET IS THE ONLY SET THAT GOT WORSE** — frozen-10 strict **−0.068** against dev **+0.066** (0.407 → 0.473) and clean test **+0.133** (0.338 → 0.471) — direct evidence for the 2026-09-13 claim that 0.925 was mostly *fitting*, now visible because the corpus changed underneath it. **TWO FINDINGS RECORDED AND HANDED TO D6, NOT FIXED** (D5's scope is the floor): **(1) `eval_heldout.py:438` reads the WRONG VARIABLE** — `assert CORPUS_VERSION == "v1"` tests the **module constant** imported from `rag.py`, not the **effective** `--corpus=` value that `main()` already parses into a local `corpus` and uses correctly two lines later, so under `--corpus=v2` the assert **never fires** and the run instead trips the frozen-10 recall guard it was written to pre-empt; the **same bug mislabels the stamp**, printing `(corpus v1)` on a v2 run in direct contradiction of the file's own docstring. D6 fixes **both occurrences** and gates v2 on `recall_strict` against a **separate** constant. **(2) `SYN:car` is the only v1→v2 refusal flip and is NOT a false refusal** — bare `car` goes 0.1141 → 0.0794 and is refused under v2, which *looks* wrong because v2 Act cl. 12 **is** "Reserved spaces … public parking lots", but **v1 never answered it correctly**: 0.1141 just cleared the floor, which let `PerDocRetriever.query`'s **entry gate** admit it to expansion, and the expanded query (`car vehicle transport parking road`) top-1'd **Constitution s. 40** (assembly and association) at 0.2126 — a junk answer produced by expansion manufacturing overlap, the exact defect that entry gate is documented to prevent. v2 refuses and its best *bare* hit is the **correct** chunk (Factsheet "Section 12 Reserved Places"); **real sentences are unaffected and improve** ("is there accessible parking for people with disabilities" **0.2157 → 0.2802**, "do I have a right to accessible public transport" 0.3180 → 0.3174). Clearing 0.0794 would mean dropping `MIN_SCORE` ~25% while the band is inverted, to rescue one bare word — so the script **reports the 34 bare keys with every flip named but does not gate them** (`classify()` carries the argument), and the open question, a *ranking* one for D6/E, is that a one-word query is too thin to reach its own correct chunk. `ablate_phase08`'s "34-key flips: none" measures a **different axis** (expansion on/off *within* one corpus) and is not contradicted. **NO BEHAVIOUR CHANGED — one new script plus three comment blocks** (`src/retrieve.py`'s calibration block and its merge-caveat paragraph, `src/rag.py`'s three deferral notes rewritten in past tense with the outcome), which is why **the whole sweep reproduces**: `eval_heldout`/`ablate_phase08`/`audit_corpus`/`eval_chat` **stdout byte-identical on BOTH arms** (v1 and `--corpus=v2`), `eval_phase06 --out=` still **`ce716fb3…5f19`**, bench PASS · 03 16/16+7/7 · 04 10/10 · 05 **137** · 09 **51** · `import app` clean, and `git diff main -- requirements.txt data/processed/disability_act_2018_full.txt` **EMPTY**. **The v2 arms still exit 1 for the recorded reasons — D5 made nothing pass**, exactly as D3/D4 left their audit FAILs visible. The 2026-09-08 calibration figures are **kept as v1-era history** in the comment block with one correction: they were measured on a different arm (k=2/doc, no `select_top`) and the **`visa` probe is unreproducible** — no such probe exists anywhere in the repo; the only "visa" in the tree is Constitution item 42 ("Passports and visas"), i.e. corpus text, not a query. **Next: D6 — re-baseline, zero quota** (flip `CORPUS_VERSION`, re-scope all three gates, fix `eval_heldout.py:438`, delete `ACT_KNOWN_ABSENT`, re-verify `conversations.json`'s 51 refs).
- **D6 PLANNED AND SPECIFIED 2026-09-19 — a PLANNING session, docs only, zero code, zero quota. NOTHING WAS EXECUTED; do not read this entry as work done.** D5 shipped (`6c4a308` + `f4b02d6`, pushed; branch `phase10/corpus-v2` unmerged; tree clean except untracked `scripts/eval_tmp.json`). This session checked D6's premises against the surviving D5 stdout captures in `D:\d5_baseline\` and **corrected three of them**, all by diffing `chat_v1.txt` against `chat_v2.txt` — none of it existed in the repo before this commit. **Finding 1 — the stamp bug is FIVE sites, not one.** D5 recorded `eval_heldout.py:438`; the same wrong read is at **`eval_heldout.py:244`, `:373`** and **`eval_chat.py:344`, `:411`** — every per-set and headline table interpolates the module constant `CORPUS_VERSION` instead of the effective `--corpus=` value, so `chat_v2.txt` prints `CORPUS_VERSION=v2` at `:1` (with the v2 corpus shape `65 · 2037 · 32` at `:2`, against v1's `62 · 2104 · 48`) and then `(corpus=v1, …)` at **`:10`, `:110` and `:197` of the same file**. The one-token fix is the `(corpus or CORPUS_VERSION)` form already correct at `eval_heldout.py:341` / `eval_chat.py:371`. **Recorded as load-bearing ordering: the stamp fix lands BEFORE the `CORPUS_VERSION` flip** — while the constant is still `"v1"` the fix is *provable* (a `--corpus=v2` run's tables must flip to `corpus=v2` while the v1 run stays stdout byte-identical, a two-sided check that catches both a missed site and an over-eager one); after the flip the buggy expression and the correct one return the same string forever and the proof is gone. **Finding 2 — `eval_chat --corpus=v2` exits PASS, and that is the problem.** `chat_v2.txt:218` reads `EVAL_CHAT: PASS` identically to v1; all 51 refs verify (`:3`); the Phase B gates (`:206-208`) are computed on **`chat_dev`, the tuning set**, and all three still pass. Underneath, on the **blind** set, `chat_test`'s **ellipsis gain class collapses `0.200 → 0.600 (+0.400)` to `0.000 → 0.000 (+0.000)`** (`:146` in both files), **pronoun becomes the gain class instead** (`0.400 → 0.400` v1, `0.400 → 0.600` v2, `:150`), topic-shift moves the other way (`0.000 → 0.333` v1, `0.667 → 0.667` v2, `:151`), and the **headline delta falls `+0.130 → +0.043`** (`:200`). The `n-strict`/`c-strict` columns are **identical to the plain ones in every one of those rows on both corpora**, which rules out the packed-ref subsidy: this is a **real ranking move**. Per turn, `CT2.t2` (`1.000/1.000 → 0.000/0.000`), `CT2.t3` and `CT4.t3` (both `0.000/1.000 → 0.000/0.000`) lost their contextualised hit while `CT1.t3` (`0.000/0.000 → 1.000/1.000`) and `CT4.t2` (`0.000/1.000 → 1.000/1.000`) gained — net roughly flat on the blind set, **class structure underneath it inverted**, harness green. **The sharpest instance is on the tuning set, where the gate looks straight at it:** `chat_dev`'s pronoun row is `0.571 → 0.714 (+0.143)` in v1 and `0.143 → 0.286 (+0.143)` in v2 (`:55`) — **byte-identical delta, level down by a factor of four** — and the gate, phrased "ctx BEATS naive", passes both times (`:207`). **A gate on a delta cannot see the level move underneath it, and D6 must not read that PASS as permission to flip.** **Finding 3 — two playbook predictions came true and can be confirmed rather than guessed:** `CT1.t1`'s `<-- FALSE REFUSAL` marker sits at `chat_v1.txt:112` and is **absent** at `chat_v2.txt:112` while its recall stays `0.000/0.000` (**refusal fixed, ranking not — both halves get reported**), and `CT7.t2`, recorded in the Phase 10 B review addendum as "unscoreable, not missed", goes **`0.000 → 1.000`** (`:130`). **Plus one gap: v2 has NO content tripwire.** `V1_CORPUS_SHA256` is pinned at `audit_corpus.py:141` and checked at `:586-597`; there is no v2 equivalent, so after the flip the **shipping** corpus would be less protected than the retired one — `V2_CORPUS_SHA256` joins D6's checklist, in the same commit as the flip. **Also newly written down:** the **blast radius** of the one-line flip (`app.py:458`, `scripts/test_phase05.py:73` and `scripts/test_phase09_ops.py:218` all call `build_corpus()` with **no argument**, plus `src/rag.py:642`, `ask()`'s own fallback — so `src/rag.py:48` silently changes what the 137-assert UI suite and the 51-assert ops suite test, and **any assert that fails there is a FINDING, not a number to relax**); the **chat-set step the playbook under-specified** (`verify_expected` already passes on v2 per `chat_v2.txt:3`, so the mechanical half is *done* and the real work is triaging each moved turn — ground truth now wrong under v2 → `"status": "retired-v2"`, stays in the file, excluded from means; ground truth still right but ranking worse → **record and hand to Phase E**, because retiring a regression is how a corpus rebuild launders itself into a win — with `recall_strict` as the discriminator, exactly as D5 used it on frozen-10, and a concrete triage list of `CT2.t2`/`CT2.t3`/`CT4.t3`/`CT1.t3`/`CT4.t2`/`CT7.t2`/`CT1.t1`); the explicit trap that **`chat_dev` is the tuning set, so re-tuning `CARRY_WINDOW`/`THIN_MAX`/`MARKER_MAX`/`QUESTION_WEIGHT` on v2 numbers SPENDS `chat_test`**, the last blind set in the project — v2 will make re-tuning look attractive precisely because `chat_dev`'s pronoun level fell while its delta held, and the 30 held-out single-turn questions were already spent this way on 2026-09-13; the **gate re-scopes as a line-numbered checklist** (factsheet `row_spanning == 0` with `packed` still printed, `audit_corpus.py:628-643` · Constitution `toc_general_out_ch8 == 0` plus the pinned 8+26 inventory, `:602-627` · `act_ref` validator scoped to the two cases it genuinely catches and **never promoted as a cross-source gate**, `:346-377` · `ACT_KNOWN_ABSENT` **DELETED not emptied**, `:126`, readers `:295`/`:297`/`:308`/`:452`/`:482`, caveats `:202-205` and `:215`); and **D6's exit criteria** (every gate green on its re-scoped metric **with the old metric still printed beside it**, since a re-scope that deletes the superseded column is indistinguishable from tuning the yardstick · v1 stdout byte-identical to `D:\d5_baseline\` via `--corpus=v1`, `eval_phase06.py` still hashing `CE716FB3…5F19` · every new guard **negative-tested by injection**, D5's gate 1 being exactly why that is non-negotiable · **`MIN_SCORE` still `0.10`** and `SYN:car` not re-opened · every moved chat turn dispositioned in writing · `scripts/baseline_v2_<date>.txt` archived). **Owner decision this session: G BEFORE E**, overriding the D → E → F → G order `CLAUDE.md` had carried until now (**`CLAUDE.md` is corrected in this same commit**, with a dated in-place note recording the previous order rather than silently swapping it) — Phase G (judge + fresh transcripts + `cross_turn_drift`) is **the only thing that measures what a user actually reads**, while E tunes retrieval that no generated answer has yet been scored against; `cross_turn_drift()` has been wired and unmeasured since Phase 10 B (`chat_v2.txt:210-212`: *"STRUCTURALLY UNAVAILABLE here — it needs a generated answer"*). **G needs a multi-day quota plan before its first call**: free tier is 20/day/model and the judge run alone is ~30, so the split (across days, and/or across `gemini-2.5-flash` and `gemini-2.5-flash-lite`'s separate pool) gets planned *before* spending, since a half-finished judge run is wasted quota. **Files: `docs/phases/12_corpus_v2.md` (D6 section — premises box · order of operations · blast radius · `V2_CORPUS_SHA256` · chat-set triage + tuning-set trap · gate checklist · exit criteria) · `HANDOFF.md` · `STATUS.md` · `LEARNING_JOURNAL.md` · `CLAUDE.md` · `docs/phases/11_chat.md`.** **Two stale-doc defects fixed in the same pass, both found by grepping for every statement of the phase order rather than only the one already flagged:** (1) `CLAUDE.md`'s working-conventions block still read *"Next up: Phase D"* with the **E → F → G** chain — now **D6 → G → E → F**, with the reversal dated and its reason in place, and the still-true *"D before E is load-bearing"* note **kept** (D → G → E still satisfies it); (2) **`CLAUDE.md`'s quota section was materially wrong and a playbook already knew it** — it read *"20 calls/day/model (`gemini-2.5-flash`, confirmed by 429)"*, naming **one** model, while `docs/phases/11_chat.md:31-36` had recorded since Phase 10 B that the measured truth is **20/day/model AND 10/minute/model with `gemini-2.5-flash` and `gemini-2.5-flash-lite` on SEPARATE pools → a real budget of 40/day**, which `ask(failover=True)` already exploits — and had assigned the fix to **Phase G**. It is discharged **early, on purpose**: the ~30-call judge run is Phase G's own first task and cannot be planned against a figure that **under-counts the budget by half** (30 does not fit in 20; it does fit in 40 across two pools, or across two days on one). `11_chat.md` keeps the original paragraph as provenance with a dated RESOLVED box beside it. No `src/`, no `scripts/`, no `data/` touched; `scripts/eval_tmp.json` left untracked; branch left unmerged. **`D:\d5_baseline\` is outside the repo and unbacked by git — all three findings exist only because those captures were kept on disk rather than summarised into prose. Do not delete it until D6 archives its own baseline.** **Next: execute D6, then Phase G.** *(order superseded later the same day — see the Phase E entry below: `D6 → E → G → F`.)*
- **PHASE E PLANNED AND SPECIFIED 2026-09-19 — `docs/phases/13_retrieval_quality.md` is NEW. A PLANNING session, docs only, zero code, zero quota; NOTHING EXECUTED.** Triggered by the honest question "the legal Q&A path is mediocre — can we fix it, on free tier?" The answer is **yes, and the diagnosis is already measured**: this is a **ranking** problem, not a corpus or a refusal problem. **THE CORE NUMBER — held-out `test` (clean, never tuned against, n=17) on corpus v2 reads `r@6 = 0.426` on the shipping arm against `r@60 = 0.735` in the pool, a `+0.309` gap the user never sees** (`D:\d5_baseline\heldout_v2.txt:109-110`); `eval_heldout.py` prints the interpretation rule in place — *"A large gap between @6 and @60 means the miss is RANKING, and a re-ranker can reach it."* **Three facts bound the work:** (1) **ceiling 0.824 on test** (found 14/17 in pool at all; dev 20/25 → 0.800), so 3 test questions are genuinely *absent* and need corpus/encoder work that is **out of E1–E4's scope** — do not chase them by widening; (2) **the floor is innocent and must not be touched** — `kept=0` is `0/8 · 0/7 · 0/5 · 0/5` across every answerable class and false refusals are **0/25 dev, 0/17 test**, so `MIN_SCORE` has nothing to do with this and D5 already re-derived it; (3) **we are barely selecting at all** — shipping is `k=3/doc` = **9 candidates for 6 slots**, two-thirds of everything retrieved is displayed, so widening the pool is a **precondition** for re-ranking rather than an alternative to it. **Worst class is `vocab-mismatch` (dev recall 0.375, precision 0.125, n=8) — the exact class the hand-written synonym map exists to fix**, unchanged by corpus v2 (cross-document 0.476, toc-trap 0.500, odd-wording 0.600). **THE SYNONYM MAP'S ONLY POSITIVE EVIDENCE IS ON THE SET IT WAS FITTED TO:** `ablate_v2.txt:15-28` prices expansion at **`0.605 → 0.666`, +0.061 mean, on frozen-10**, and inside that mean it is carried by **Q9 (+0.500)** and **Q8 (+0.250)**, is **actively harmful on Q2 (−0.143)** and **inert on 6/10** — which, with D5's `SYN:car` finding (expansion *manufactured* overlap and top-1'd `Constitution s. 40` for a parking query), makes **"expansion off entirely" a legitimate third arm** rather than an unthinkable one. **THE PLAN — five steps, all $0, zero Gemini quota, zero new runtime dependency, default path still fully offline:** **E1** separate the three conflated budgets and widen the candidate pool to **`k=20/doc` (60)**, shipped **measure-only first** so the widened pool with today's ordering is a *control arm* and widening is never attributable to the same commit as re-ranking; **E2** **BM25 re-rank INSIDE the existing gate** — cosine and `MIN_SCORE` stay as the admission gate and only the admitted set is re-ordered, because BM25 scores are unbounded and swapping the scorer outright would invalidate the `src/retrieve.py:26-44` calibration and **change refusal behaviour**; it is the highest-confidence item because TF-IDF cosine handles **term saturation** and **length normalisation** badly and Phase D widened the chunk-length spread to **2.75×** (`ACT_V2_SIZE = 1100` vs `CONST_SIZE = 400`), which is precisely what BM25's `k1`/`b` exist for; **E3** replace the hand map with a **corpus-derived** `synonyms_auto.json` built **without looking at the eval questions** (building it the old way — keeping entries by their effect on Q5/Q7/Q8/Q9 — is what produced 0.925-vs-0.420 in the first place), validated **once** on held-out data in a **three-arm ablation incl. no-expansion**, with willingness to ship **neither**; **E4** an *optional, evidence-gated* **static** word-embedding blend (GloVe/fastText-style matrix lookup + mean-pool ≈ **2–4 MB**, pure numpy via the existing scikit-learn, offline, fits Render's 512 MB) aimed at the semantic gap nothing else closes — **explicitly NOT dense retrieval**, and if an implementation starts needing ONNX/torch/FAISS/network it has left the step; blended with BM25, never replacing it, because static vectors are **weak on exact legal terminology** (`section 34` vs `section 43` are near-identical); **E5** re-baseline the full sweep, archive `scripts/baseline_phaseE_<date>.txt`, read `test` **once at the end**, then make the M2 decision. **M2 (dense via Gemini embeddings) is GATED BEHIND E5, NOT SUPERSEDED — and its premises were RE-CHECKED before that box was written, not assumed broken.** M2 already owns the query-side problem honestly (embed at tier 1, cache on the normalised query, tiered fallback, a visible notice that the question is sent to Google, a hard-offline toggle, and `CLAUDE.md`'s offline invariant **amended in the same commit**) — a deliberate documented trade. What changed is that it can now be **priced**: (a) its quota premise is **still unmeasured** because `scripts/probe_embed_quota.py` has never run and free-tier embedding limits are unpublished — **run the probe regardless of the decision**; (b) the privacy cost is **specific to this population**, whose questions describe disability, abuse and begging coercion; (c) the residual gain may be small, since E1–E4 target the same in-pool headroom and the absent 3/17 may be out of dense retrieval's reach too. **PHASE F (M4) FLAGGED FOR RE-EXAMINATION BEFORE SCHEDULING:** it stands on the same wall that superseded `08_retrieval_upgrades.md` steps 4/5 — **Colab gives free *training*, not free *serving***, and a fine-tuned transformer still cannot embed a query at request time under `requirements.txt` (512 MB Render free tier); **the one variant that survives is fine-tuning a STATIC table**, i.e. E4's artifact, which is what F should probably become. **ORDER REVERSED: `D6 → E → G → F`**, overturning the G-before-E call taken *earlier the same day*, on **resource-asymmetry** grounds — **E costs zero quota and *improves* the system; G costs ~30 calls, most of a day's entire 40/day budget, and only *measures* it**, so judging first spends the scarce resource on a system about to change and needs re-running; the safety argument for G-first is weaker than it looked because AI prose is **opt-in and defaults OFF**, so unmeasured cross-turn drift is not reaching users. The earlier decision is **kept in `HANDOFF.md` and `LEARNING_JOURNAL.md`, annotated not erased**, because the reasoning is the record. **Files: `docs/phases/13_retrieval_quality.md` (NEW, full playbook: diagnosis · E1–E5 · what free tier genuinely blocks · traps · exit criteria) · `docs/phases/09_evidence_and_generation.md` (step 3 → MOVED box) · `docs/phases/10_corpus_rebuild_and_dense.md` (M1 ABSORBED + k-sizing corrected · M2 GATED · M4 RE-EXAMINE) · `CLAUDE.md` · `HANDOFF.md` · `STATUS.md` · `LEARNING_JOURNAL.md`.** No `src/`, no `scripts/`, no `data/` touched; `scripts/eval_tmp.json` left untracked; branch left unmerged. **Next: execute D6, then Phase E.**
- **PHASE D COMPLETE — D6 EXECUTED 2026-09-19 (seven commits `02f42f8` → `5838beb`, branch `phase10/corpus-v2`, zero Gemini calls, branch STILL UNMERGED — merging auto-deploys to Render and that call is the user's).** `CORPUS_VERSION = "v2"` is now the **default** (`src/rag.py:48`); v1 is retired as the default, **not deleted** — every harness still takes `--corpus=v1` and reproduces its published numbers from the same commit. **What the flip ships:** the Act's **clause 38**, absent from v1's processed text entirely because the v1 scan records one physical page twice, and an end to cl.38's tail being served under an `[Act cl. 39]` tag — **a citation-integrity defect that stays live in `main` until this branch merges**. **THE ORDER HELD AND PAID FOR ITSELF:** the five-site stamp fix landed *first*, while the constant still read `"v1"`, which made it a two-sided proof — `eval_heldout.py` and `eval_chat.py` stayed **byte-identical** to the D5 captures in `D:\d5_baseline\`, while `--corpus=v2` flipped exactly the 3 predicted stamp lines each with **no number moving**. **The fifth site turned out to be a guard that had been FAILING OPEN:** `eval_heldout.py:438` asserts the run is on v1 *because* plain recall is not comparable across corpus versions, but it read the module constant, so `--corpus=v2` saw `"v1"` and let the comparison through — meaning **D5's published `frozen-10 recall 0.666 vs recorded baseline 0.925 (corpus v1): FAIL` is a v2 number measured against a v1 baseline, mislabelled v1, by the very guard written to refuse it**; the legitimate comparison is the strict one D5 also published (**0.701 → 0.633**). **EVERY GATE NOW HAS A v2 ARM ON A LEGITIMATE METRIC, each printing the metric it does NOT gate** (dropping the superseded column is indistinguishable from tuning the yardstick): frozen-10 and `ablate_phase08` gate **`recall_strict ≥ 0.632738`** — *not* the `0.633` the table prints, because the true value is `0.63273809523809521` and 3dp display rounds it **up**, so pinning the printed number would have failed the run it was derived from; the Constitution gates **`toc_general_out_ch8 == 0`** plus the pinned **8 + 26** inventory that covers the stated Chapter VIII proxy hole; the factsheet gates **`row_spanning == 0`**. **`V2_CORPUS_SHA256` and `V2_RESULTS_SHA256` pinned** — without them the *shipping* corpus would have been less protected than the retired one, and `eval_phase06`'s digest gate (`if (corpus or CORPUS_VERSION) == "v1"`) would have gone **silent** the moment the default flipped. **`ACT_KNOWN_ABSENT` DELETED, not emptied** (an empty set reads as "we checked and nothing is absent"), after checking the line the code itself said to check first: manifest **58/58 located, zero degradation flags**; both its entries were false — cl.38 absent from the v1 *text* only, cl.40 never absent at all. **`act_ref` SCOPED to contradictions only, never promoted as a rate** — the two "independent" sources share an upstream, so 65/65 is near-tautological for a single-clause chunk; the review's alternative (validate against the Arrangement *title*) was **rejected in writing** because the manifest's titles come from the Arrangement too, swapping one shared upstream for another while looking independent. **EVERYTHING NEW WAS NEGATIVE-TESTED BY INJECTION — and one injection PASSED, which was a defect in my gate rather than in the test:** I had added a second `act_ref_validator()` call while the report already made one, so it ran twice over 65 chunks and the first pass **absorbed the injected contradiction**; deduplicated (65 calls, not 130) it then fails correctly. **An injection that passes is a result to investigate, not a box to tick.** Threshold injections only test the comparison, so both new metrics were also run against real defective data and **discriminate**: `toc_general_out_ch8` **v1 7 / v2 0**, `row_spanning` **v1 26/48 / v2 0/32**. **THE CHAT-SET TRIAGE INVERTED ITS OWN PREMISE — this is the session's main finding.** D6 was handed "`chat_test`'s ellipsis gain class collapsed `+0.400 → 0.000` and a green harness is hiding a real regression." Three turns carried that entire gain and **all three of v1's hits are artifacts**, established by reading the corpus rather than reasoning about it: **`CT2.t2`/`CT2.t3` expect `act2018:[8]` but were authored off v1's `[Act cl. 8]` chunk, which CARRIED CLAUSE SEVEN'S SUBSECTION (3)** — the officer-approval offence — while v2 files that text under `[Act cl. 7]` where it belongs and v2's cl.8 (*Complaint of inaccessibility*) puts liability on "a relevant authority in charge", not an approving officer; **that is the same defect class as the `[Act cl. 39]` bug this entire phase exists to fix, found inside the eval set, where it had been scoring turns correct against mis-attributed law since 2026-09-15**. Both are `"status": "retired-v2"` — **retired, NOT edited**, because correcting `[8]` to `[7]` would silently convert a blind turn into one authored against v2. **`CT4.t3`'s ground truth is CORRECT and was KEPT**: v1 scored it 1.000 only because cl.25 sat inside an 800-char **packed** `cl. 25,26,27` chunk containing "queue" and "accommodation" — its *neighbours'* words, carried in by contextualisation from turns 1–2 — so **the chunk was retrieved for a different clause's text and credited to clause 25**; v2's 305-char cl.25 chunk contains neither word, and naive rank is **40 (v1) vs 41 (v2)**, i.e. the rebuild did *not* hurt, while contextualised rank is **5 vs 104**. **⚠ CONSEQUENCE THAT OUTLIVES D6: `recall_strict` does NOT neutralise the packed-ref subsidy in the `ctx` arm.** It stops one chunk satisfying two expected refs (*scoring*); it cannot see a chunk **retrieved** because of text belonging to another clause — so D5's "strict == plain on every class row, therefore not the packed subsidy" inference is **wrong for the retrieval side**. Net: on the 3 surviving ellipsis turns v1 reads `0.000 → 0.333` and v2 `0.000 → 0.000`, **and that 0.333 IS the artifact** — ellipsis gain on the blind set is **zero on both corpora**, and **0 turns were retired for getting harder** (retiring a regression is how a corpus rebuild launders itself into a win). **`CT7.t2`'s prediction HELD exactly**: unscoreable on v1, now `0.000 → 1.000` with the correct chunk at pool rank **2** where v1 had it at 9; caveat marked resolved and **kept, not deleted**, because a prediction that held is evidence. **RETIREMENT MECHANISM BUILT FROM SCRATCH** (`status` was unhandled anywhere in the repo): `chatset.load_chat_set()` validates it and **requires a non-empty `retired_reason`**; `eval_chat` scores a retired turn `None` at a *single* point so every existing `mean()` excludes it, the per-turn table flags it loudly, and retired turns are **still replayed as history** since removing them would change what later turns resolve against — negative-tested three ways (reason removed / blank / bogus status, all AssertionError). **BLAST RADIUS VERIFIED, NOT ASSUMED:** the four no-argument `build_corpus()` callers (`app.py:458` the user path, `test_phase05.py:73`, `test_phase09_ops.py:218`, `ask()`'s fallback) all still pass — **137/137 and ALL PASS, unchanged**, and `import app` is clean. **v1 REPRODUCIBILITY:** `eval_heldout --corpus=v1` byte-identical, `eval_phase06 --corpus=v1` still `ce716fb3…edf5f19`; **three files diverge deliberately and enumerated** — `audit_corpus` v1 (2 hunks: cl.40 moves to "NOT citable, parseable" where it belongs, cl.38 stops claiming "the pixels do not exist", which was false), `ablate_phase08` v1 (+2 lines: it had **NO corpus stamp at all**, a sixth site of the stamp class and worse than a wrong one, plus the ungated strict column), and `eval_chat` v1 (the two retired turns — the retirement is **global by construction** because the expected refs name the wrong clause *of the Act*, not merely of v2). No number moved in the first two; no exit code changed anywhere. **BASELINE ARCHIVED: `scripts/baseline_v2_2026-09-19.txt`, all five harnesses GREEN** — `audit_corpus PASS · eval_heldout PASS · eval_chat PASS · ablate PASS · eval_phase06 sha256 10751076…ed97e057`; heldout frozen-10 `0.666`/strict `0.633`, dev `0.473`, test `0.471`; `chat_test 0.524 → 0.571 (+0.048, n=21)`, `chat_dev 0.500 → 0.607 (+0.107, n=28)`. **`MIN_SCORE` still `0.10`, untouched; `SYN:car` not re-opened.** **HANDED TO PHASE E BY NAME:** `CT4.t3` — cl.25 is **outside the 60-candidate pool entirely** (rank **104** at k=200), so **E1's `k=20/doc` widening does NOT reach it**, which must be known *before* E1 is judged on it; the mechanism is **length** (v2's clause-aligned chunks are short — 305 chars here — and TF-IDF's length handling works against them), which is **E2's BM25 rationale now carrying direct evidence** · `CT1.t1` — false refusal **fixed**, ranking **not** (naive rank 20 → 22) · `chat_test`'s ellipsis class is now **thin at 3 scoreable turns**, a real limit on what it can support · **the Phase B gates are STILL computed on `chat_dev`**, so `eval_chat` exits PASS while `chat_test` prints `GAIN CLASS DID NOT IMPROVE` — **that critique stands**, and D6 deliberately did not touch it because it is a harness-design question. **Next: Phase E — `docs/phases/13_retrieval_quality.md`, unchanged and now unblocked.**
