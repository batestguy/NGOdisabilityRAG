# STATUS — current snapshot (details in HANDOFF.md + docs/phases/)

- **Env:** `C:\conda-envs\drlca-rag` only.
- **Done:** OCR 27/27 (clauses 1–58, PARTs I–X) · retrieval baseline 10/10 · env pinned · docs pass.
- **Phase 07 prep 2026-09-09 (superseded — see LIVE line below):** requirements slimmed + clean-venv proven; README + ASCII arch; NAB/NNAD HIGH 10/10; spec audit in journal; key-audit clean.
- **Phase 02 DONE 2026-09-09 (v2 + fix-A runs) + fixA2 COMPLETE 2026-09-10:** v2 12/12 (8/10 cited; Q9 PARTIAL s.33→s.34, Q10 false refusal); fix-A run 8/12 (quota 20/day/model confirmed by 429; Q8/Q9/R1/R2 honest-pending) — Q10 VARIANCE RESOLVED (verbatim s.46 quotes, PASS), Q1-Q7 stable (Q4 +padding note), Q5 correct-refusal x3. fixA2 2026-09-10 12/12 (reviewer SEND-BACK dispositions recorded): Q9 s.33 REMOVED but s.34 omission remains, Q3 recovered (cl.29,30 5%), Q8 five-years, Q10 PARTIAL (s.46 x2 correct + s.39 misattr of High-Court text — TOC-trap class), R1/R2 verbatim DRAC refusal, Q5 correct-refusal x4. Offline: recall 0.800 PASS, faith 1.000 mechanical (overstates Q10), reverse_rel 0.630 FAIL gated (short-answer artifact, judge arbiter). Flagged rerun `eval_phase06_results_fixA2-flagged_2026-09-10.json`: faith 0.967 PASS (Q10 s.39 flagged), pointer swapped to fixA2.
- **Phase 03 DONE 2026-09-08:** 10/10 rows verified (2 pruned), 16/16 matrix + 7/7 statics PASS (reviewer: ship-with-notes). Residual: single-token fuzzy misroutes death/blend → Phase 04 must confirm on fuzzy flag. Next: `docs/phases/04_intent_router.md`.
- **Phase 04 DONE 2026-09-08:** keyword router 10/10 (exit needs 8), clarify path proven, mean latency ~0.6ms (reviewer: ship-with-notes + fixes applied: cue anchoring, singleton guard, tie policy). LLM router rejected (keyword suffices, quota preserved). Next: `docs/phases/05_accessible_ui.md`.
- **Phase 05 DONE 2026-09-08 (+styling pass) + dark-alert fix 2026-09-09:** app.py live-verified in Chromium light+dark (legal/help flows, routing trap, citations, helplines; 360px banner OK); .streamlit/config.toml light/dark Nigerian-green theme; watermark bundled (static/watermark.jpg, CC BY 4.0); scripts/test_phase05.py 104/104 (grew 80→94→96→104 across sessions; +scrims/stacking/watcher/HC-button/greeting asserts); boot HTTP 200. Residuals FIXED: (1) dark stAlert translucency → opaque override (#45491f, 9.1:1); (2) Brave white-text/sidebar report → opaque sidebar (z-index 2 over main context) + 0.88 main sheet + opaque excerpt cards; light/HC verified coherent. Font unchanged by design. Human-only left: NVDA, physical keyboard run, live mic/read-aloud, Render cold-start. Next: `docs/phases/06_ragas_eval.md`.
- **Corpus:** Act full 40K + factsheet 21K + Constitution 529K chars. `_SAMPLE_DO_NOT_CITE.txt` never citable.
- **Weak spot:** Constitution queries fixed by const size 400 (Q9 0.133→0.167 PASS, mean 0.221→0.275). Watch: constitution now 95% of joint index — per-doc routing is a Phase 02 follow-up.
- **Phase 07 deploy LIVE 2026-09-10 (Render):** service `srv-dah26opt0dsc73e9a350` (free, python 3.11, Oregon) at **https://ngodisabilityrag.onrender.com** — HTTP 200, browser-verified banner + help query (NAB, helplines first; 15-min sleep/wake cycle not yet observed). HF Spaces dropped (Streamlit SDK deprecated → Docker/PRO only). Streamlit Cloud still optional 2nd link.
- Git: `main` pushed clean 2026-09-10. Deploy audit + offline suites re-verified 2026-09-10: bench PASS, 03 16/16+7/7, 04 10/10, 05 104/104, boot import OK.
- **Hello-bug fix 2026-09-10:** bare greetings hit the refusal wall (live-reproduced) → `is_greeting()` in router + greeting panel in app (wins even over forced buttons, zero retrieval/LLM calls); 04 greeting probes + 05 asserts green; live-verified on Render.
- **Phase 06 CLOSED 2026-09-11 (judge arbitrated):** judge `gemini-2.5-flash-lite` on generator `gemini-2.5-flash` output (21 verdicts, artifact `scripts/judge_phase06_results_gemini-2.5-flash-lite_2026-09-11.json`). reverse_rel 0.630 FAIL = **metric artifact**, retired not chased: deterministic ceiling analysis caps a *perfect* system at **0.852** vs a 0.80 gate (Q3/Q8 hard-capped 0.333 by per-doc merging), and the judge rates 9/9 answers relevant — proxy-vs-judge agreement only **2/9**. Caveat recorded: all 9 judge scores were exactly 1.0 (assent bias), so the **arithmetic** carries the verdict, not the model. Q3 thinness confirmed a REAL prompt/generation defect (→ Phase 09); Q8 adequate. Judge limits logged (conflated Factsheet `Section 39` with Constitution `s. 39`; misread the `general` tag). Open + not claimed: two-run flakiness check, manual-100% (Q10 s.39 unsupported).
- **Phase 08 steps 1/2/3/6a DONE 2026-09-11, LIVE on Render (`main` @ `fcd2129`):** synonym map · cite-constrain fix-B · judge · answer cache. **recall 0.800 → 0.925** (Q5 0.000→0.250 blind spot opened; Q8 + Q9 0.500→1.000 unplanned wins from real `transitional`/`transitory` + `dignity`/`degrading` mismatches). **faith_audited 0.967 → 0.867 is the improvement, not a regression**: the old figure leaned on a hand-written `MANUAL_FLAGS` list, now `MANUAL_FLAGS == []` and Q10's s.39 misattribution is caught mechanically (auto 1.000→0.667), independently confirmed by the judge. Q5 faith 1.000→0.000 is a **frozen-transcript pool effect**, not a quality drop — the eval recomputes retrieval live, so the pre-M1 refusal is no longer justified now that `act2018:1,2` retrieve; **0.867 therefore understates current quality**. `MIN_SCORE`, `app.py`, `data/`, config.toml untouched; **`requirements.txt` byte-identical to pre-M1 (blob `dc312fa`, verified)**. Live smoke zero-quota: banner ✔, Q5 "penalties" → `[Act cl. 2]`/`[Act cl. 1]` matching local to 3dp (proves new retrieval deployed) ✔, help → helplines-first + NAB ✔, greeting `"hi"` beats a forced Legal button ✔.
- **Demo assets 2026-09-11:** 5 frames @1925×881 from the LIVE app + `docs/demo_storyboard.md` (frames 02/03 re-shot — first pass captured the page scrolled to the top and showed neither the citation tags nor the NGO list). Storyboard records the frames were shot at `db3a1fc`, i.e. they show the UX contract, **not** the Phase 08 retrieval gain.
- **Phase 09 PLANNED 2026-09-11, not started (`docs/phases/09_evidence_and_generation.md`):** a planning pass on the deferred Phase 08 steps 4/5 found three code facts that reorder the backlog. (1) **Steps 4/5 as written cannot reach a real user** — both need a model at *query* time (step 4 a cross-encoder; step 5 to embed the incoming query, which prebuilt chunk vectors do not solve) and `requirements.txt` bans ONNX/FAISS, so "flag-gated" means OFF in production. **Superseded**, not deferred again. (2) **Retrieval is no longer the bottleneck** — recall 0.925 vs a 0.75 gate, while the judge confirmed Q3 thinness is a REAL prompt defect (Q3 recall 1.000). (3) **The synonym map is fitted to the set that scores it** — entries kept/deleted by their effect on Q5/Q7/Q8/Q9 across only 10 questions, so 0.925 cannot distinguish generalization from memorization. Owner confirmed all four workstreams under a **runtime-shippable-only** constraint: (1) ops hardening (`--no-cache` + daily-cap-only flash-lite failover → real budget is 40/day, not 20), (2) `data/eval/questions.json` + ~30 held-out questions incl. first-ever false-refusal/false-answer measurement (frozen 10Q stays frozen AND separate), (3) BM25 re-rank *inside* the cosine gate + offline-computed `synonyms_auto.json` (both wins of steps 4/5, zero query-time deps), (4) Q3 answer-shape floor + named reverse_rel fix in its own commit + fresh 12-call generation pass.
- **Phase 09 steps 1+2 DONE 2026-09-13 (zero Gemini calls; PRs #9 `phase09/ops-hardening`, #10 `phase09/evidence-base`):** **THE FINDING — held-out recall 0.420 (n=25) vs frozen-10 0.925, delta −0.505.** The 2026-09-11 suspicion ("the synonym map is fitted to the set that scores it") is now measured: 0.925 was mostly fitting. Per class the shape is the tell — **vocab-mismatch 0.312** (n=8), cross-document 0.429, toc-trap 0.500, odd-wording 0.500: *the worst class is the one the synonym map exists to fix*, so **the hand-written map does not generalize**. Two numbers measured for the first time: **false-refusal 0/25 = 0.0%** on answerable held-out questions (frozen-10 0/10 — the floor is not denying help to PWDs; ranking is the problem, not the gate), and **gate-level false-answer 5/5 = 100%** of off-corpus questions clear `MIN_SCORE` — reported **ungated with the caveat, and `MIN_SCORE` untouched** (weak-overlap floor, not a semantic filter; bands inverted per `src/retrieve.py:26-44`; "sourdough" 0.125 / "maritime" 0.318 predate this work; the semantic layer is the strict-prompt `NO_ANSWER_SENTENCE` path and costs quota to measure). Step 1: `FALLBACK_MODEL=gemini-2.5-flash-lite` (separate free-tier pool → real budget **40/day, not 20**), `is_per_minute_429`/`retry_delay` moved verbatim into `src/rag.py` (judge imports+aliases, output byte-unchanged), `ask(use_cache=, failover=)` records `model_used` while `route["model"]` keeps its old meaning; failover is opt-in, fires once, **never on a per-minute 429**. New `scripts/test_phase09_ops.py` **38/38, zero network** — failover proven by *injection* (proving it live means deliberately exhausting a daily cap). Step 2: `data/eval/questions.json` (10 frozen + 30 held-out), `scripts/evalset.py`, `scripts/eval_heldout.py`; `eval_phase06.py` output **byte-identical** across the refactor (16335 B both, baseline `scripts/eval_p09pre.json`). `load_questions()` **KEPT, not retired** (contra the playbook): two independent sources that must agree > one nobody checks. Verified no held-out score is inflated by the `const_ref` schedule-item artifact. Corpus gap found: **Act cl.19 is uncitable** (OCR `19.1` → no `act2018` ref carries 19). `MIN_SCORE`, `app.py`, `requirements.txt`, `data/processed/` untouched.
- **Pending:** Phase 09 steps 3 (BM25 re-rank inside the gate + offline `synonyms_auto.json` — now has a real target *and* an uncontaminated yardstick) and 4 (Q3 generation defect), in that order. Quota: two-run flakiness check **unblocked but NOT run** (24 calls; `--no-cache` landed 2026-09-13); + AI-expander live smoke. Owner: GIF encode + NVDA/keyboard/mic gates + LinkedIn. Housekeeping: stray 0-byte `D:NGORAG_review_judge.diff` in repo root needs manual removal (permission-blocked; in no commit). NGO 10/10 verified (NAB/NNAD HIGH 2026-09-09).
- **Phase 10 B (chat-core) DONE 2026-09-15 (zero Gemini calls, branch `phase10/chat-core`):** DRLCA becomes a chatbot, and the eval came first. *"So can they fire me?"* has no disability term and no statutory term, so **ellipsis is a RETRIEVAL problem, not a UI one** — which is why the multi-turn set was built and measured *before* the chat UI and before corpus v2/dense (tuning those on single-turn questions only would optimise a query distribution the chatbot never issues). New: `data/eval/conversations.json` (22 conversations / 62 turns / 51 refs, all corpus-verified), `scripts/chatset.py`, `src/chat.py` (`contextualise`, `merge_help_slots`, `TurnPayload`, `cross_turn_drift`), `history=` on `ask()`/`build_prompt()`, `scripts/eval_chat.py`, `scripts/baseline_chat_2026-09-15.txt`. **`chat_test` authored BLIND before `src/chat.py` existed** (10 conv / 27 turns — left at 27, not topped up, because adding turns once behaviour is visible is how a blind set stops being blind). **Results:** chat_dev **0.464 → 0.571 (+0.107)**, chat_test **0.435 → 0.565 (+0.130)**; ellipsis **0.000 → 0.333** (dev) / **0.200 → 0.600** (test); `direct` control **+0.000** both sets; recall@60 (pool k=20/doc) **0.714 → 0.929** / **0.783 → 0.913**. Help slots (`"I'm deaf"` … `"anywhere in Kano?"`) **3/5 → 5/5** and **1/2 → 2/2**. **False refusals FELL** (dev 1/28 → 0/28, test 3/23 → 1/23) with **0 contextualisation-induced** — the one retrieval number `eval_chat.py` fails on. **The new chat-only trap did NOT fire:** off-corpus-after-legal clears `MIN_SCORE` **2/2 in both arms on both sets, delta 0.000** (one probe's top score *fell* 0.2755 → 0.2434); that matches the published single-turn 5/5 and is a property of the inverted-band floor, **`MIN_SCORE` untouched**. **Recorded, not tuned:** chat_test's pronoun class did **not** improve (0.400 → 0.400), and a blind test-set note (`CT6.t3`) predicted a carry that behaves otherwise — the wrong note stays, because editing it after seeing behaviour is what spent the 30 held-out questions on 2026-09-13. Thresholds (`CARRY_WINDOW=2/THIN_MAX=3/MARKER_MAX=4/QUESTION_WEIGHT=2`) chosen on **chat_dev only**. `test_phase09_ops.py` 38 → **51 asserts**: `history=None` renders a **byte-identical** prompt (cache keys on the whole prompt) and history renders **BEFORE** the final `"Question: "` line (**privacy** — `_cache_write` stores only that line, and this population discloses abuse). Chat has its own `CHAT_PROMPT_VERSION="chat-cite-strict-v1"` + rule 8: citable evidence is **only this turn's excerpts**. **Unmeasured on purpose:** cross-turn citation drift (needs a generated answer → quota; wired, runs in Phase G). Untouched: `MIN_SCORE`, `src/router.py` (**still stateless**), `app.py`, `requirements.txt`, `data/processed/`, `questions.json`; `eval_phase06.py` output still `CE716FB3…5F19`; 01/03/04/05(104)/ablate_08/audit/heldout all green. Playbook: `docs/phases/11_chat.md`. **Next: Phase C — the chat UI (`phase10/chat-ui`).**
- **Phase 10 C (chat UI) DONE 2026-09-16 (zero Gemini calls, branch `phase10/chat-ui`):** Phase B's measured gain (+0.107 chat_dev / +0.130 chat_test strict recall) was **unreachable by a user** — `app.py` was still a single-turn form. This step spends it and measures nothing new: **no retrieval, scoring, corpus or prompt change**, so `eval_chat.py` and `eval_heldout.py` were required to reproduce `368f083` exactly and did (**byte-identical stdout**); `eval_phase06.py` still hashes `CE716FB3…5F19`; `git diff --stat main -- requirements.txt` **empty**. `app.py` splits into **`render_turn`** (computes + renders a new turn) / **`replay_turn`** (renders a stored `TurnPayload` — **no retrieval, no router, no network**, asserted) / `render_history` / a `st.chat_input`-driven `run()`, with `render_excerpts`/`render_ai_expander`/`render_help_records`/`render_plain_caption` shared so a live turn and a replayed turn are the same pixels. `src/router.py` gained **one defaulted arg** (`_help_payload(..., slots=None)`) and **stays stateless** — accumulated slots arrive as an argument, never stored. **`test_phase05.py` 104 → 137 asserts, all green**, 6 rescoped with an inline note each (`:32-39` `.index()`-ed into the whole module; `def render_helpline_banner` now sits *after* `def render_help` in file order, so the module-wide check would have failed on layout while the rendered order was unchanged — they now scope to `render_turn`'s body). **Two disclosed deviations from the plan:** (1) a **second** defaulted `TurnPayload` field `help_payload` — without it, replaying a help turn forces a live router call on every rerun, the exact thing `replay_turn` exists to prevent; neither new field is read by any eval, so no number can move; (2) the per-turn `answer_legal(..., use_llm=False)` **probe was deleted** — it existed only to print the prompt-version string and cost **a whole second retrieval per turn per rerun**; replaced by `_prompt_id()`, which reads `PROMPT_VERSION`/`CHAT_PROMPT_VERSION` from `src/rag.py`. **Width/pool depth was NOT folded in** (the older plan had it here): `offline_legal_hits`' docstring forbids a display path that slices differently from `ask()`, both harnesses measure `k=3/doc, top_n=6`, and Phase B measured the change at **test +0.000** — it moves in **Phase E**, with the harnesses, where the dense arm needs it. The **display** split (3 excerpts inline + rest in one expander) did land. **Privacy: history is `st.session_state` only.** New `scripts/.quota_log.json` holds **counts only** (`{"YYYY-MM-DD": {"model": 3}}`), fails open like `_cache_write`, gitignored; sidebar says *"THIS INSTANCE SINCE RESTART"*, never *"today"* (ephemeral FS + 15-min sleep). Cache replays are not counted — they spend no quota. **Browser-verified** (Chrome/DevTools, no key spent): 4-turn conversation with an ellipsis (*"so can they fire me?"* → `unbound-pronoun:they`), a clarify turn, and a narrowing (*"I'm deaf"* … *"anywhere in Kano?"* → NNAD from a slot carried two turns); banner at page top **and** leading every assistant turn; per-turn read-aloud **genuinely isolated** (each `components.html` is its own iframe — verified by reading `#drlca-speech` per frame); both AI toggles OFF; Clear conversation works; 360px no overflow; dark + HC coherent. **After the whole session neither `.answer_cache.json` nor `.quota_log.json` existed** — the offline path wrote nothing. Three AI-failure paths (no key / 429 daily cap / network down) proven **headlessly at zero quota**: `answer` stays `None` (pending, never faked), no quota recorded, one plain-language warning, no traceback. **Latent bug fixed in passing:** `try_voice_input()` wrote `session_state["q"]` *after* the `q`-keyed widget rendered, which Streamlit forbids; it now writes `voice_draft`, copied into `q` before instantiation. **Observed, not changed:** dark+HC together leaves the sidebar `#010409` not `#000` (equal specificity, dark rule emitted later; ~19:1 contrast, predates this phase; `accessibility_css()` untouched). Untouched: `MIN_SCORE`, `data/`, `requirements.txt`, `.streamlit/config.toml`, `read_aloud_html()` (byte-identical). Owner-gated: NVDA, physical keyboard run, live mic, AI-expander live smoke. **Fresh-eyes review: no blocking issues, verdict ship** — it re-ran 03/04/05 + `import app`, traced every `session_state[key]=` write against every widget `key=`, and confirmed history reaches no file. Two fixes taken (104 → **137**): (a) `_prompt_id()` decided multi-turn from `turn_index > 0` while `ask()` branches on `rag._render_history()` being non-empty — equivalent today only by caller discipline, since `history_for_prompt()` applies `CARRY_WINDOW` and drops blank turns; now `_multi_turn(prior)` calls the same two functions `ask()` calls, with 3 asserts pinning it (**the comment was the defect, the code was lucky**); (b) a **pre-existing tautological assert** — `no-wide-columns-for-banner` ended in `or True` and had been padding every published count since Phase 05 — rewritten to check that every `render_helpline_banner()` *call site* sits at 4-space top level, never inside a `with col:`. Both evals re-run after the fixes: still byte-identical to `368f083`. **Next: Phase D — corpus v2 (`docs/phases/10_corpus_rebuild_and_dense.md`).**
- **Phase D PLANNED 2026-09-16 — docs only, zero code, zero quota (`docs/phases/12_corpus_v2.md`, NEW):** a planning session that went looking for M3's premises and **falsified two of them**, which is why Phase D needed its own playbook instead of executing M3 of `10_corpus_rebuild_and_dense.md`. **Finding 1 — the Act gap was a SOURCING failure, not an OCR failure, and the published "no OCR can recover it" claim is WRONG.** The owner's untested lead downloaded to `6document.pdf` (14.1 MB) is *Official Gazette No. **10**, Vol. 106, 21 Jan 2019, Act No. 2, pages **A97–A122*** (the old playbook guessed "No. 11"): 27 pages, 0 embedded text chars, and — unlike the existing copy — **no duplicate adjacent page**. Targeted OCR (`rapidocr_onnxruntime`, dpi 200) of gazette pages 13/14/15 recovered `38.TheCommissionshall-` (A109), its `(e)…(r)` tail (A110) and `40.—(1) There shall be an Executive Secretary` (A111). **Then the sharper correction: both bodies were in the v1 processed text all along** — `(1)TheCommissionshall--` at **L614** (numeral `38.` OCR'd as `(1)`) and `(1) There shall be an Executive Secretary for the Commission who shall-` at **L545** (numeral dropped). The duplicate page 5–6 **is** real (adjacent-page cosine **0.978** vs a 0.794 runner-up, dpi 100 / 16×16 mean-pooled / **mean-centred** — centring is load-bearing, uncentred saturates at 1.000 for every pair and detects nothing) — it just did not cost cl.38 or cl.40. **`ACT_KNOWN_ABSENT = {38, 40}` (`scripts/audit_corpus.py:80`) is a false constant** currently excusing two clauses from the coverage gate; Phase D **deletes it, not empties it**. Scope limit stated in the playbook: only pages 1/13/14/15 of 27 were OCRed, so "all 58 clauses present" is a **D2 verification task, not a finding**. **Finding 2 — the Constitution needs only the cheap fix:** all **99** uncitable Constitution chunks are Arrangement-of-Sections material (88 under 60 words; all 11 at ≥60 words are *also* numbered title listings — `"236 Practice and procedure"`, `"89 Power as to matters of evidence"`), so **zero substantive body text is uncitable** and excluding the Arrangement pages takes `general` 4.7% → ≈0 and deletes the `toc-trap` class **without touching `CONST_SIZE = 400`**. M3's 2104 → ~500 section-unit re-extract — the riskiest change in the plan — **is not needed for the gate** and defers to Phase E. **Finding 3 — M3's order is backwards:** M2 ships a **per-chunk** embedding artifact keyed on a corpus sha256, so rebuilding the corpus after it invalidates every vector; **corpus first**, which is what `HANDOFF.md` already assumed by putting D before E without ever saying why. Also measured: the Act's **16 packed refs are all consecutive runs** (`cl. 3,4,5` … `cl. 56,57`) = pure 800-char cutting, mechanically fixable; the Factsheet's 19 are **disordered** (`Section 51,40`, `Section 50,45,54`) = `recursive_split(500/50)` cutting the S/N table mid-row. Playbook ships **D0–D6** with the refusal-floor recalibration **mandatory, not conditional** (dropping ~99 TOC chunks moves the IDF space and clause-aligned chunks run longer than 800; `MIN_SCORE` **may move down, may not move up**), `_is_toc_fragment` **kept and demoted to a lint assertion** rather than deleted, and the thing M3 never anticipated: **`conversations.json`'s 51 refs were verified against v1** and must be re-verified in D6. **Files: `docs/phases/12_corpus_v2.md` (new) + amendment boxes in `10_corpus_rebuild_and_dense.md` (M3 SUPERSEDED, M1/M2 reordered after D, cl.38/40 premise FALSIFIED — kept, not deleted) + `HANDOFF.md` + `STATUS.md` + `LEARNING_JOURNAL.md`.** No `src/`, no `scripts/`, no `data/` touched; `audit_corpus.py` still **AUDIT_CORPUS: PASS** (v1 tripwire, 9/9 ok) and `import app` still clean, re-run after the doc edits. **Next: execute Phase D on `phase10/corpus-v2`.** **Owner decision 2026-09-16: the 14 MB gazette PDF GETS COMMITTED** to `data/raw/` (consistent with the already-tracked 5.2 MB Act and 8.5 MB PLAC Constitution; never read at boot, so the slim Render runtime is untouched; accepted cost is a permanent large blob in history, so it lands in its own D1 commit). Still open: whether to push/PR the chat work before going **three unpushed branches deep**.
- **Phase D correction + design pass 2026-09-17 — docs only, zero code, zero quota:** the Phase D entry above is **partly wrong and the correction is load-bearing.** `4bcc763`'s Finding 1 claimed cl.38's body was in the v1 text at **L614**; **L614 is clause 48** (L612 reads `48.`; the Arrangement at L74 reads `48.Annual estimate and expenditure.` — the marginal note wrapped at L613/L615; v1 continues `(a) cause tobekept accounts and records` where the gazette continues `(a) formulate and implement policies`; `grep "formulate and implement"` returns **nothing** in v1). **cl.38 is genuinely ABSENT from v1** and is recovered from the **gazette** (A109–A110), not from v1; **cl.40's row was correct** (body at L545, bounded by `39.` L524 / `41.` L555). The match was made on string similarity **without reading the next line** — the same failure mode the 2026-09-16 journal entry was written to warn about, committed one day later. **The conclusion survives:** `ACT_KNOWN_ABSENT = {38, 40}` still retires (cl.40 never absent, cl.38 gazette-recoverable), all 58 clauses still reachable in v2 — but deletion moves to **D6**, after the re-OCR, not D2. **Three new findings from the re-check, all strengthening v2:** (1) **v1's cl.37 is silently corrupted** — a physical page is missing at the `===== PAGE 14 =====` boundary (L500); raw OCR p13 ends at cl.37(b), p14 opens mid-list at cl.38(j), so the duplicate p5/p6 **did** cost a page, just not cl.40's; (2) **a LIVE citation-integrity defect in `main`** — Act chunk 33 is reffed **`cl. 39`** and opens with cl.38's `(o)`–`(r)` incl. *"procure assistive devices for all disability types"*, so the app can serve cl.38's text under an `[Act cl. 39]` tag and `verify_citations()` passes it mechanically (chunk 31 `cl. 36,37` is clean, chunk 32 is `general`); (3) **v1 L542 reads `PARTVII`** where the Arrangement (L64) and gazette say **PART VIII**, and **v1's Arrangement truncates at L77 `51.Power to acquire land.`** — `audit_corpus.py:64-66` cites it as the source of `ACT_CLAUSES = range(1, 59)` but it lacks 52–58 (right number, wrong source), so **D2's Arrangement go/no-go gate must run on the gazette's front matter, which has not been OCRed yet** — now D2's first task rather than an assumption. **Also folded in: the D1+D2 implementation design**, so the next session executes rather than re-derives — `Chunk` gains a defaulted `path` field (**verified**: `defaults=` preserves 3-arg positional construction, so `rag.py:228,235,242` are untouched; the real risk is `len()` 3→4, `*chunk` splatting and 3-tuple unpacking, with a six-pattern `rg` sweep and the `bench_phase01.py:244,246,250` near-miss recorded); `build_corpus(version=None)` is a **pure relocation** of three blocks with **no memoization, no dict reordering** (insertion order breaks `PerDocRetriever` ties at `retrieve.py:331-338`) and **no `version=` on `ask()`**; a three-tier caller policy over the **14** call sites (never-flagged incl. `router.py:221`, whose `_retriever` global is **unkeyed**, and `rag.py:642`, which is `ask()`'s own fallback build; `--corpus=` equals-form-only; untouched quota-bound); and two prose claims become real gates — a `corpus_sha256()` fingerprint (`V1_EXPECTED`'s nine integers cannot see text **moving between chunks** at constant count) and an enforced `CE716FB3…5F19` assert **read back from disk**, because `write_text` emits CRLF on Windows and the published digest is of the CRLF bytes (**verified**: 594 CRLF pairs) — disclosed as Windows-specific and as adding one ` sha256=…` stdout line. D2 design: **three layers** so only `scripts/ocr_gazette.py` imports pymupdf/rapidocr (→ stdlib `parse_act_v2.py` → JSON-only `_act_chunks_v2()`), which is what keeps `requirements.txt` honest; **`scripts/ocr_local.py` is a trap — never import it** (calls `main()` at module scope, no `__main__` guard, unconditionally overwrites `data/processed/disability_act_2018_full.txt` at `:59`) → extract `ocrlib.py` **keeping `box` coordinates** (v1's assembler discards them) plus a guard and `--force`; **marginal notes are geometry-primary, title-as-validator** (measured on v1 OCR JSON p13: body `x0 107.4–363.6` vs notes `x0 1448.5–1466.2`; pure title-anchoring **fails on wrapped notes** — `Cessation of` / `office.` are separate OCR lines matching neither title alone — so group by y-adjacency first and detect the note side **per page**); **`dedupe_pages()` must NOT run on v2** (its salvage branch `load.py:347-363` appends unmatched lines onto the kept twin — a corruption mechanism on a clean source) and becomes an assertion that `detect_duplicate_pages(...) == []`; clause locator is a **monotonic cursor** (kills the `section 27 of the Interpretation Act` false-positive class structurally, no heuristic — and would have rejected the L614 match above) plus **two required anchors**, with `TITLE_WEAK`/`NUMERAL_MISSING` flagged never silent and no-anchor a hard failure; an **Arrangement go/no-go gate** with a written-in-advance fallback (titles from marginal notes, `"title_source"` recorded, discrepancies reported) and **never author a title**; **cross-check predictions written before the first run** (cl.38 absent · cl.37 large disagreement · cl.38(p)–(r) vs the `cl. 39` chunk · cl.40 agrees · PART VII/VIII disagrees; anything else is a parser bug) and **text-level only**, never page position, since the editions paginate differently; `ACT_V2_SIZE = 1100` as a **new** constant, explicitly **not** `ACT_SIZE` and **not tuned in D2** (longer chunks lower every cosine — that is D5's recalibration); `ref = "cl. %d" % n` from the parser so **packed refs are structurally impossible**; and the chunk header is the statutory heading (`38. Functions of the Commission.`) — the marginal note **restored to the position the printer moved it out of**, which makes `act_ref()` resolve v2 natively and the D2 validator a tight equality check. Playbook verification gains `git diff main -- data/processed/disability_act_2018_full.txt` **must stay EMPTY** — the playbook guarded `requirements.txt` but never the v1 TXT, the one artifact `ocr_local.py` can silently clobber. **Also recorded: `CLAUDE.md`'s "Act section-aware 800" is misleading** — `chunk.SECTION_RE` matches **4 times** in the entire cleaned Act text, all at 89.8%+ of the document (Second Schedule + Forms), so v1's Act split is effectively `recursive_split(text, 800)` for the whole operative body; not a bug (it is part of the byte-identical v1 path) but it means D2's clause-aligned chunking is a **total replacement** of the Act splitter, not a refinement. **Files: `docs/phases/12_corpus_v2.md` (Finding 1 corrected + Findings 1a/1b/1c + D1/D2 implementation notes) · `HANDOFF.md` (Finding 1 block + standing fact, itemised per clause) · `docs/phases/10_corpus_rebuild_and_dense.md` (amendment box) · `LEARNING_JOURNAL.md` (2026-09-16 entry boxed as half-wrong + new 2026-09-17 entry) · `STATUS.md` · `README.md` (stale since 2026-09-10 — status, chat usage, architecture, eval list) · `CLAUDE.md` (Phase D pointer + chat architecture + the §-aware caveat) · `AGENTS.md`.** No `src/`, no `scripts/`, no `data/` touched; `audit_corpus.py` still **AUDIT_CORPUS: PASS** (9/9 v1 tripwire ok), `eval_phase06.py` still hashes **`CE716FB3…5F19`**, `import app` still clean, `test_phase05.py` 137/137. **Fresh-eyes review: verdict ship-with-fixes, both fixes taken and disclosed in place** — it independently re-derived all six corrections and every design fact (all reproduce exactly) and found **two wrong line references in the new D1 notes**: the `bench_phase01.py` near-miss was cited as `:159,251,253` (those are `sum(...)` comprehensions and an `assert`; the real unpacks are **`:244,246,250`**), and the caller-policy table said **13** call sites while omitting **`src/rag.py:642`** — `ask()`'s own fallback `PerDocRetriever(build_corpus())`, i.e. **the user path's default corpus build**, the single caller the table most needed — plus `app.py:457`/`router.py:220` cited the `from rag import …` lines rather than the calls at `:458`/`:221`. Corrected to **14** with dated in-place notes rather than silently, in a session whose entire subject is unverified references. **Next: execute Phase D on `phase10/corpus-v2`, starting with the gazette Arrangement OCR + go/no-go gate.**
- **Phase 10 B review addendum 2026-09-15:** fresh-eyes review re-derived every quoted number from a clean run (byte-identical to the committed baseline) — all match. One real defect found: **`CT7.t2` is UNSCOREABLE, not missed** — it expects `act2018:[5]` but the chunk carrying the Act First Schedule list is reffed **`general`** (`ref_nums("general")` is empty), so both arms retrieve that exact chunk at ranks 5-6 and still score 0.000. `chatset.verify_expected()` passed it because an unrelated `cl. 3,4,5` chunk carries a 5 — it proves the *number* exists, not that the right *text* is reachable under it; that limit is now documented in the function, together with a second one (Constitution section numbers **collide across chapters** — s.36/18/34/40/42 — so `(doc_id, number)` matching can produce a false POSITIVE; verified not live in either set today). **The expectation was NOT edited** — the correction would have moved chat_test's pronoun class *in our favour*, i.e. the exact class recorded as "did not improve"; a dated addendum went into the turn's `note` instead. Net: a **second named instance** of the uncitable-chunk class (alongside "Act cl.19 is uncitable" and the 25/62 uncitable Act chunks) and one more piece of evidence for **Phase D**, whose audit gate is `ref=="general"` ≤1% per doc.
